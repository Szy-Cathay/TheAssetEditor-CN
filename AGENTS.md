# Codex 项目规则

## 适用范围

- 本文件适用于整个 `TheAssetEditor-CN` 仓库。
- 用户当前指令优先于本文件；更深目录中的 `AGENTS.md` 仅覆盖其目录范围。
- 本项目是面向中国大陆用户的 Asset Editor 中文专用下游版本。

## 沟通与决策

- 解释、计划、进度和最终回复使用中文。
- 代码注释使用英文，除非所在文件已经采用其他语言。
- 编辑前说明目标、假设、涉及文件、风险和验证方法。
- 不确定时明确指出疑点并询问，不暗自猜测。
- 存在多种方案时说明取舍，并优先推荐最简单且风险最低的方案。
- 多步骤任务先给出简短计划，每一步必须有可检查的成功标准。
- 用直白语言说明修改和风险，不假设用户能够安全审查代码。
- 简单任务的计划保留在对话中；未经要求，不新增设计、计划或流程文档。

## 实施原则

- 使用能完整解决问题的最少代码，不添加未要求的功能、配置或抽象。
- 只修改与当前任务直接相关的文件和代码行。
- 不顺手重构、格式化、清理或删除无关代码。
- 只移除由本次修改导致未使用的 import、变量、函数或资源。
- 匹配现有架构和代码风格，不为个人偏好改写已有实现。
- 优先使用现有框架、辅助类和结构化 API，不做脆弱的字符串拼接解析。
- 修复缺陷时，条件允许应先添加能够复现问题的聚焦测试。
- 除非用户明确要求，绝不删除、覆盖、回滚或移动用户的工作。

## 国区版身份边界

- 保持产品名和程序集身份 `AssetEditor.CN`。
- 主程序必须保持为 `AssetEditor.CN.exe`。
- 更新器必须保持为 `AssetEditor.CN.Updater.exe`。
- 用户数据必须隔离在 `%USERPROFILE%\AssetEditor.CN`。
- 本地应用数据、缓存、日志、更新目录和备份不得与上游版本共用。
- IPC 管道必须保持为 `AssetEditor.CN.Ipc`。
- 更新源必须保持为 `Szy-Cathay/TheAssetEditor-CN`。
- 用户可见界面仅保留中文；新增界面文本应接入 `AssetEditor/Language_Cn.json`。
- 不重新引入语言选择器、英文或法文运行时本地化入口。
- 保留现有 C# 命名空间和 `RootNamespace`；不得进行全仓命名空间替换。
- 不把旧版目录、IPC 或上游仓库重新作为运行时身份。
- 上游来源、版权、提交历史和说明性链接必须保留，不得当作旧身份残留批量删除。
- 版本号以项目文件为准；主程序和更新器的版本变更必须同步检查。

## 仓库与技术约定

- 主解决方案是 `AssetEditor.CN.sln`。
- 主程序位于 `AssetEditor/`，是 Windows WPF 应用，目标框架为 `net10.0-windows`。
- `Editors/` 包含编辑器模块，`GameWorld/` 包含三维与渲染功能。
- `Shared/` 包含共享核心、格式、资源和 UI，`Testing/` 包含主要测试项目。
- 仓库仍有较旧目标框架的项目；未经明确要求，不批量升级目标框架或依赖。
- 遵循根目录 `.editorconfig`：使用空格缩进，C# 通常为 4 空格并保持 CRLF。
- C# 文件遵循现有 using 排序、命名、可空引用和分析器规则。
- 不对整个仓库运行自动格式化，只格式化本次实际修改的代码。

## 构建与测试

- 完整验证应与 GitHub Actions 的 Release 配置一致：
  - `dotnet restore AssetEditor.CN.sln`
  - `dotnet build AssetEditor.CN.sln --configuration Release --no-restore`
  - `dotnet test AssetEditor.CN.sln --configuration Release --no-build --no-restore --verbosity normal`
- 开发过程中可以先运行受影响项目或测试类，但不能用局部测试冒充完整验证。
- 测试资源路径必须从当前配置和目标框架推导，不得硬编码 `Debug` 输出目录。
- 纯文档或规则文件修改通常只需内容检查和 `git diff --check`，无需运行全量 .NET 测试。
- 本地化修改至少检查中文资源键、构建结果和实际界面加载。
- 渲染或材质修改必须同时验证源码契约、CPU 到 Effect 参数传递及离屏像素行为。
- 发布链路修改必须使用干净发布目录验证主程序、更新器和运行时文件。
- 不得把本地构建或发布目录准备完成描述为公网发布完成。
- 报告验证时列出实际命令、退出状态、通过数量及未验证部分。

## Git 与副作用安全

- 修改前检查 `git status`，识别并保留已有工作区改动。
- 不使用 `git reset --hard`、强制检出或其他破坏性命令。
- 大范围或高风险改动先说明范围并取得确认，再使用独立的 `codex/` 分支。
- 未经明确要求，不提交、推送、创建 PR、发布版本、上传代码或部署服务。
- 扩大 GitHub 权限前必须取得明确授权，尤其是 `workflow` scope。
- 不购买云资源、不暴露本地服务、不修改生产环境或外部访问权限。
- 对删除、清理、迁移、覆盖和权限操作，先审计并说明精确目标与可恢复性。

## 工具与子智能体

- 外部工具不可用时，先搜索合适的 MCP、插件、connector 或 skill。
- 找到合适工具时优先补齐工具链，并说明是否需要重启对话。
- 未找到工具时应明确说明；取得用户确认后才能采用手动替代方案。
- 只有用户明确要求使用、创建或派遣子智能体时，才允许调用子智能体。

## 完成标准

- 修改必须完成实现、适当验证和清晰结果说明，不能停在分析或半成品。
- 最终回复列出修改文件、行为变化、验证结果和剩余风险。
- 用户要求“修复、复测并推送”时，完成标准包括本地测试、提交、推送及 GitHub Actions 最终状态。
- 未实际执行或无法确认的步骤必须明确标注，不得推测为成功。
