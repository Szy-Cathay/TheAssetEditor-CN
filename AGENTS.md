# Codex 项目规则

## 适用范围与用户画像

- 本文件适用于整个 `TheAssetEditor-CN` 仓库。
- 用户当前指令优先于本文件；更深目录中的 `AGENTS.md` 仅覆盖其目录范围。
- 本项目是面向中国大陆用户的 Asset Editor 中文专用下游版本。
- 用户是完全不会编程的中文用户。
- 使用直白中文解释结果；必须使用技术术语时，紧接着说明它的实际含义和影响。
- Codex 能自行执行和验证的操作，不把命令丢给用户代为运行。

## 沟通与编辑前确认

- 解释、计划、进度和最终回复使用中文。
- 代码注释使用英文，除非所在文件已经采用其他语言。
- 不确定时明确指出疑点，不隐藏困惑、不暗自猜测。
- 存在歧义时列出可能解释，并一次只询问一个关键问题。
- 存在多种方案时说明取舍，优先推荐最简单且风险最低的方案。
- 编辑前说明目标、假设、涉及文件、风险和验证方法。
- 假设用户不能安全审查代码，因此必须用直白语言解释修改范围和副作用。
- 多步骤任务先给出简短计划，每一步包含可检查的成功标准。
- 用户已经明确授权的操作不重复确认，但不得把授权扩大到其他操作或路径。
- 简单任务的计划保留在对话中；未经要求，不新增设计、计划或流程文档。

## 实施原则

- 使用能完整解决问题的最少代码，不添加未要求的功能、配置或抽象。
- 不为一次性代码建立新框架，不为不可能发生的情况添加复杂处理。
- 如果 50 行可以清楚解决问题，不写成 200 行。
- 只修改与当前任务直接相关的文件和代码行。
- 不顺手重构、格式化、清理或删除无关代码。
- 如果发现无关问题，只报告，不擅自修复。
- 只移除由本次修改导致未使用的 import、变量、函数或资源。
- 匹配现有架构和代码风格，不按个人偏好改写已有实现。
- 优先使用现有框架、辅助类和结构化 API，不做脆弱的字符串拼接解析。
- 修复缺陷时，条件允许应先添加能够复现问题的测试，再让测试通过。
- 每一行修改都必须能够直接追溯到用户请求。
- 除非用户明确要求，绝不删除、覆盖、回滚或移动用户的工作。
- 用户要求“审查”或“检查”时，先按严重程度报告问题；未要求修复时不直接编辑。

## Git 分支与工作区保护

- 每次操作前检查当前仓库、分支和完整 `git status`。
- 识别并保留已有修改，不覆盖、不夹带、不回滚用户或其他任务的工作。
- 凡会修改仓库文件，默认在第一次编辑前从干净的目标分支创建独立 `codex/` 分支。
- 只有用户明确要求在当前分支工作，或专用发布流程明确要求使用 `master` 时，才可例外。
- 不允许先在 `master` 修改，再补建分支作为常规做法。
- 不使用 `git reset --hard`、强制检出或其他破坏性命令。
- 未经明确授权，不修改 GitHub 权限、分支保护、仓库设置或 `workflow` scope。
- 不使用强制推送、自动合并或绕过分支保护，除非用户针对该操作明确授权。

## Git 操作授权边界

- 未经用户明确要求，不得自动执行本地提交、推送、创建 PR 或合并 PR。
- “本地提交”“推送分支”“创建 PR”“合并 PR”是不同阶段。
- 用户只要求本地提交时，不得继续推送。
- 用户明确要求推送当前修改时，可执行必要的本地提交和推送，但不得自动创建或合并 PR。
- 用户明确要求创建 PR 时，可执行必要的本地提交、推送和创建 PR，但不得自动合并。
- 用户明确要求合并 PR 时，必须先检查 PR、目标分支和必需检查，再执行合并。
- 用户对后续阶段的授权可以包含必要的前置阶段，但不得包含更后续的阶段。
- 推送功能分支不等于完成合并，不得描述为“已经合并”。
- 完成合并后必须核验目标分支、本地和远端提交，以及用户要求包含的文件。
- 用户说“不修改版本号”时必须保持版本文件不变，不把普通推送或合并扩展成发布任务。

## 国区版身份边界

- 保持产品名和程序集身份 `AssetEditor.CN`。
- 主程序必须保持为 `AssetEditor.CN.exe`。
- 更新器必须保持为 `AssetEditor.CN.Updater.exe`。
- 用户数据必须隔离在 `%USERPROFILE%\AssetEditor.CN`。
- 本地应用数据、配置、缓存、日志、更新目录和备份不得与上游版本共用。
- IPC 管道必须保持为 `AssetEditor.CN.Ipc`。
- 程序内置更新检查与安装包下载源必须保持为 Gitee 仓库 `szy-cathay/AssetEditor-CN-Downloads`；源码、问题跟踪和 GitHub Release 仍使用 `Szy-Cathay/TheAssetEditor-CN`。
- 用户可见界面仅保留中文；新增界面文本必须接入 `AssetEditor/Language_Cn.json`。
- 不重新引入语言选择器、英文或法文运行时本地化入口。
- 保留现有 C# 命名空间和 `RootNamespace`，不得进行全仓命名空间替换。
- 不把旧版目录、IPC、程序名或上游仓库重新作为运行时身份。
- 上游来源、版权、提交历史和说明性链接必须保留，不得作为旧身份残留批量删除。
- 版本变更必须同步检查主程序、更新器和 `README.md`，不得全局替换版本字符串。

## AI 项目上下文

- 代码、项目引用和测试是当前实现的事实来源；仓库文档只补充代码不能稳定表达的约束、术语、入口和设计取舍。
- 不要默认通读全部文档。领域词汇消歧读取 `CONTEXT.md`；跨模块定位读取 `docs/architecture.md`；文件夹工程或本地 Git 读取 `docs/folder-project-version-control.md`；IPC 读取 `docs/asseteditor-ipc.md`。完整路由见 `docs/README.md`。
- 新增、修改、审查或验收 KitbashEditor、模型选择/变换/覆盖层、场景树、保存或 Kitbash 子工具前，必须完整读取并遵循 `docs/kitbash-editor.md`。
- 新增、修改、审查或验收超级视图（SuperView）、Animation META 预览、元数据时间/空间编辑或其双文档保存前，必须完整读取并遵循 `docs/superview.md`。
- 新增、修改、审查或验收任何用户界面、窗口、控件、主题、图标、动画或加载进度前，必须完整读取并遵循 `docs/ui-design-system.md`；该文件是本仓库唯一长期 UI 规范。特别必须执行其中的“Agent 公共组件复用协议”：先搜索、再复用，未满足准入条件不得新增公共控件。
- 只有稳定契约、领域含义、关键入口或已知限制改变时才同步更新文档；内部重构、测试数量、性能快照、分支名和提交 SHA 不写入长期上下文。
- 文档结论必须用当前代码或测试核验；历史对话、记忆、提交标题、PR 描述和上游文档只能用于定位，不能直接证明当前行为。
- 不整包复制上游 `AiDocumentation`，也不为可由当前代码直接重建的类清单、方法说明或普通流程重复写文档。

## 仓库与编码约定

- 主解决方案是 `AssetEditor.CN.sln`。
- 主程序位于 `AssetEditor/`，是目标框架为 `net10.0-windows` 的 Windows WPF 应用。
- `Editors/` 包含编辑器模块，`GameWorld/` 包含三维和渲染功能。
- `Shared/` 包含共享核心、格式、资源和 UI，`Testing/` 包含主要测试项目。
- 当前所有项目统一目标框架为 `net10.0-windows`；框架、SDK 或依赖的全仓变更属于高影响任务，未经明确要求不得批量升级。
- 在 Windows 环境中使用 PowerShell；文本和文件搜索优先使用 `rg`。
- 文件操作使用明确路径和结构化 API，不跨 shell 拼接删除、移动或覆盖命令。
- 遵循根目录 `.editorconfig`，包括空格缩进、命名、可空引用和分析器规则。
- C# 通常使用 4 空格和 CRLF；代码注释保持简短且使用英文。
- 不对整个仓库运行自动格式化，只格式化本次实际修改的代码。

## 架构与模块边界

- `AssetEditor` 是组合根；新增模块注册应沿用各项目的 `DependencyInjectionContainer` 和 `RegisterTools`，不要在视图或业务代码中临时拼装另一套服务容器。
- `Shared/SharedCore` 放置无 WPF 业务界面的核心服务与模型，`Shared/GameFiles` 放置游戏格式，`Shared/SharedUI` 放置跨模块、无业务含义的通用 WPF 组件。
- `Shared.Ui` 只能引用 `Shared/` 内项目，不得反向依赖 `AssetEditor`、`Editors` 或具体业务编辑器；已有 `SharedUiArchitectureTests` 是这条边界的自动门禁。
- 只有被多个模块真实复用、无业务含义且可独立验证的 UI 才进入 `Shared.Ui`；对话框等通用依赖优先走 `IStandardDialogs` 等现有抽象。
- 比较上游 `D:\TheAssetEditor` 时必须实时核对两边 HEAD、分支、工作区、行为、项目引用、本地化和测试；文件名相同或旧记忆不能证明功能等价。

## Pack 与文件夹工程

- 保持三层状态严格分离：编辑器内存中的未保存编辑、文件夹工程磁盘/Git 工作树、Git 暂存区与提交历史。不得把其中一层的状态伪装成另一层。
- `Stage` 只选择下次提交内容，`Stash` 才会收起工作树修改；“生成 Pack”是从文件夹工程生成输出文件，不得隐式替代保存编辑器内容或 Git 提交。
- 文件夹工程以 `aeproject.cn.json` 为当前配置文件，并要求工程根目录拥有有效的本地 Git 仓库；兼容旧配置只用于迁移，不得重新写回旧身份。
- 输出 Pack 必须位于文件夹工程目录之外；资源路径必须是受 `FolderProjectPathPolicy` 约束的相对路径，不能绕过重解析点、保留名、控制文件和元数据目录检查。
- 会改写大量磁盘文件的 Git 操作必须通过现有协调器关闭/重新加载工程并保留空目录状态；大型工程验收必须分别测量 Git、磁盘、重新对账和 UI 刷新阶段。

## 构建与验证

- 验证范围按修改风险决定，但不能用局部验证冒充完整验证。
- 完整 Release 验证命令为：
  - `dotnet restore AssetEditor.CN.sln`
  - `dotnet build AssetEditor.CN.sln --configuration Release --no-restore`
  - `dotnet test AssetEditor.CN.sln --configuration Release --no-build --no-restore --verbosity normal`
- 开发过程中可以先运行受影响项目、测试类或测试用例。
- 测试资源路径必须从当前构建配置和目标框架推导，不得硬编码 `Debug` 输出目录。
- 纯文档或规则文件修改通常只需内容检查、关键条目扫描和 `git diff --check`。
- 本地化修改必须检查中文资源键、Release 构建和实际中文界面加载。
- UI 修改必须实际启动或渲染验证，不能只依据代码推测显示正确。
- 渲染或材质修改必须验证源码契约、CPU 到 Effect 参数传递和离屏像素行为。
- 发布链路修改必须使用全新的暂存目录验证主程序、更新器和运行时文件。
- 不得把本地构建或本地发布目录准备完成描述为公网发布完成。
- 报告验证时列出实际命令、退出状态、通过和跳过数量，以及未验证部分。
- WPF 测试若涉及 `Application`、主题资源、`ResourceDictionary` 或 `Freezable`，使用 `Testing/AssetEditorTests/WpfTestApplicationHost.cs` 的单一 STA/Dispatcher 宿主；不要在各测试线程反复创建或搬运全局 WPF 状态。
- 修改测试代码后，先重新构建受影响测试项目；不得用指向旧程序集的 `--no-build` 结果判断新代码。
- `SharedUiArchitectureTests`、文件夹工程服务/ViewModel 测试和更新器路径安全测试属于架构门禁；修改对应边界时必须运行相关测试，不能只做编译检查。

## 版本发布

- 用户要求发布指定版本时，必须使用 `release-asseteditor-cn` Skill。
- 严格使用用户给出的版本号，不自行递增、降级或改写。
- 明确的指定版本发布请求，授权执行该 Skill 固定范围内必要的版本修改、提交、推送、标签、部署和 GitHub Release。
- 发布授权不包含创建 PR、合并 PR、强制推送、扩大权限或操作其他目录和仓库。
- 任一构建、测试、暂存发布、部署、压缩包、CI 或远端核验失败时立即停止。
- 只有本地验证、推送、GitHub Actions 和 GitHub Release 回读全部成功后，才能报告发布完成。

## 外部工具

- 外部工具不可用时，先搜索合适的 MCP、插件、connector 或 skill。
- 找到合适工具时优先补齐工具链，并说明是否需要重启对话。
- 未找到工具时明确说明；得到用户确认后才能采用手动替代方案。

## 任务完成后的收尾

- **触发口令**：只有当用户将“本次任务完成”“开始任务收尾”“执行任务收尾”或语义等价的话术作为明确操作指令，要求结束当前任务并开始收尾时，才执行本节。仅讨论、引用或举例这些话术不触发收尾；Agent 自行判断“实现完成”也只用于报告进度，不能代替用户触发口令。
- **执行顺序**：收到触发口令后，先完成本节，再进行本任务任何尚未完成的提交、推送、创建 PR 或合并，使收尾产生的跟踪文件变化进入同一交付。提交、推送、创建 PR、合并、发布或测试通过本身只表示对应步骤完成，不触发收尾。若触发口令出现时本任务已经合并，先说明收尾会产生新的未合并变化并等待用户决定，不得静默执行。
- 用户已持续授权清理以下固定范围，无需每次重复确认：仓库内所有 `bin`、`obj`、`TestResults`、`.vs`，本次任务产生的具名临时目录，`artifacts`，以及 `graphify-out/cache`、`graphify-out/.graphify_python`、`graphify-out/.vocab.txt`。如果 `artifacts` 中存在其他未完成任务仍需使用的内容，只删除能明确归属本任务的子目录，并报告保留项；扩大到其他路径仍需单独授权。
- 清理前必须检查 `AssetEditor.CN`、更新器、Visual Studio、`dotnet`、MSBuild 和测试进程；检查每个目标的绝对路径、仓库包含关系、重解析点和 Git 跟踪状态。相关进程仍在运行、路径无法可靠归属或目标包含跟踪文件时停止清理并报告，不得使用 `git clean -ffdx` 或同类全仓删除命令。
- 清理必须保留源码与用户未提交工作、`.git`、`.user`、备份、全局 NuGet 缓存，以及 Graphify 的跟踪输出、`manifest.json`、`.graphify_root`、`.graphify_learning.json`、`memory` 和 `reflections`。
- 临时文件清理完成后，必须完整读取并使用 `graphify` Skill，从最终代码状态更新代码知识图谱。已有 `graphify-out/graph.json` 时优先执行增量更新；只有图谱缺失、损坏或增量更新无法产生可信结果时才完整重建。
- 图谱更新完成后，再删除本轮 Graphify 生成的纯缓存和临时文件，同时保留图谱、报告、清单、标签、记忆和反思。图谱更新产生的仓库修改仍遵守上文 Git 分支与操作授权边界，并在本任务后续提交、推送、PR 或合并中一并交付；收尾本身不自动扩大 Git 操作授权。
- 收尾完成标准是：清理目标均不存在，保护项仍存在，代码知识图谱成功更新且可读取，Git 状态中的变化全部可解释。任一步失败都必须明确报告，不能把本次任务描述为已经完整收尾。

## 完成标准

- 修改必须完成实现、适当验证和清晰结果说明，不能停在分析或半成品。
- 最终回复列出修改文件、行为变化、验证结果和剩余风险。
- 用户要求“修复、复测并推送”时，完成标准包括本地验证、必要提交、推送和 GitHub Actions 最终状态。
- 用户要求创建或合并 PR 时，必须报告 PR 状态、目标分支和远端最终结果。
- 未执行、无法确认或依赖旧记录的步骤必须明确标注，不得推测为成功。

## Agent skills

### Issue tracker

本仓库使用 GitHub Issues 管理需求和问题。详见 `docs/agents/issue-tracker.md`。

### Triage labels

问题分流使用五种标准角色及其默认英文标签名。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用 single-context（单上下文）领域文档结构。详见 `docs/agents/domain.md`。
