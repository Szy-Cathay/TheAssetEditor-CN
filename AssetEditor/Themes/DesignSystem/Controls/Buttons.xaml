<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="AeButton.KeyboardFocusVisual" TargetType="{x:Type Control}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Control}">
                    <Border
                        Margin="-2"
                        BorderBrush="{DynamicResource AeBrush.Accent}"
                        BorderThickness="2"
                        CornerRadius="{StaticResource AeRadius.Surface}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AeButton.Base" TargetType="{x:Type Button}">
        <Setter Property="MinWidth" Value="64" />
        <Setter Property="Height" Value="{StaticResource AeSize.ControlHeight}" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource AeBrush.Surface3}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.BorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource AeButton.KeyboardFocusVisual}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid
                        x:Name="InteractionSurface"
                        RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <ScaleTransform
                                x:Name="InteractionScale"
                                ScaleX="1"
                                ScaleY="1" />
                        </Grid.RenderTransform>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition
                                        From="Normal"
                                        GeneratedDuration="{StaticResource AeMotion.Hover}"
                                        To="MouseOver" />
                                    <VisualTransition
                                        GeneratedDuration="{StaticResource AeMotion.Pressed}"
                                        To="Pressed" />
                                    <VisualTransition GeneratedDuration="{StaticResource AeMotion.Hover}" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="InteractionScale"
                                            Storyboard.TargetProperty="ScaleX"
                                            To="1" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="InteractionScale"
                                            Storyboard.TargetProperty="ScaleY"
                                            To="1" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="InteractionScale"
                                            Storyboard.TargetProperty="ScaleX"
                                            To="1" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="InteractionScale"
                                            Storyboard.TargetProperty="ScaleY"
                                            To="1" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="InteractionScale"
                                            Storyboard.TargetProperty="ScaleX"
                                            To="0.98" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="InteractionScale"
                                            Storyboard.TargetProperty="ScaleY"
                                            To="0.98" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border
                            x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource AeRadius.Control}" />
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Chrome" Property="Opacity" Value="0.45" />
                            <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextMuted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="AeButton.Primary"
        BasedOn="{StaticResource AeButton.Base}"
        TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource AeBrush.Accent}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.Accent}" />
        <Setter Property="Foreground" Value="{DynamicResource AeBrush.Canvas}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource AeBrush.AccentHover}" />
                <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.AccentHover}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource AeBrush.Accent}" />
                <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.AccentHover}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="AeButton.Secondary"
        BasedOn="{StaticResource AeButton.Base}"
        TargetType="{x:Type Button}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource AeBrush.SurfaceHover}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource AeBrush.Surface2}" />
                <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.Accent}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="AeButton.Quiet"
        BasedOn="{StaticResource AeButton.Base}"
        TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextSecondary}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource AeBrush.SurfaceHover}" />
                <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextPrimary}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource AeBrush.Surface2}" />
                <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextPrimary}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="AeButton.Danger"
        BasedOn="{StaticResource AeButton.Base}"
        TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource AeBrush.Danger}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.Danger}" />
        <Setter Property="Foreground" Value="{DynamicResource AeBrush.Canvas}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="0.9" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Opacity" Value="0.78" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="AeButton.Icon"
        BasedOn="{StaticResource AeButton.Quiet}"
        TargetType="{x:Type Button}">
        <Setter Property="Width" Value="{StaticResource AeSize.ControlHeight}" />
        <Setter Property="MinWidth" Value="{StaticResource AeSize.ControlHeight}" />
        <Setter Property="Padding" Value="0" />
    </Style>
</ResourceDictionary>
