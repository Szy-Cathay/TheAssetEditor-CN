<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style
        x:Key="AeWorkflow.SettingsNavigationItem"
        TargetType="{x:Type TabItem}">
        <Setter Property="Width" Value="144" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextSecondary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource AeFocus.Keyboard}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Grid Background="{TemplateBinding Background}">
                        <Border
                            x:Name="HoverOverlay"
                            Background="{DynamicResource AeBrush.SurfaceHover}"
                            IsHitTestVisible="False"
                            Opacity="0" />
                        <Border
                            x:Name="SelectionIndicator"
                            Width="2"
                            Margin="0,5,0,5"
                            HorizontalAlignment="Left"
                            Background="{DynamicResource AeBrush.Accent}"
                            CornerRadius="0,1,1,0"
                            Visibility="Collapsed" />
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            ContentSource="Header"
                            RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextPrimary}" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity" To="1" Duration="{StaticResource AeMotion.Hover}" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource AeMotion.Hover}" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AeBrush.AccentSoft}" />
                            <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextPrimary}" />
                            <Setter TargetName="SelectionIndicator" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.42" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="AeWorkflow.SettingsNavigation"
        TargetType="{x:Type TabControl}">
        <Setter Property="TabStripPlacement" Value="Left" />
        <Setter Property="Background" Value="{DynamicResource AeBrush.Canvas}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.Border}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="144" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border
                            Background="{DynamicResource AeBrush.Surface1}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="0,0,1,0">
                            <TabPanel
                                x:Name="HeaderPanel"
                                Margin="0,4"
                                IsItemsHost="True"
                                KeyboardNavigation.TabIndex="1" />
                        </Border>
                        <Border
                            Grid.Column="1"
                            Background="{TemplateBinding Background}">
                            <ContentPresenter
                                x:Name="PART_SelectedContentHost"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                ContentSource="SelectedContent"
                                KeyboardNavigation.TabIndex="2" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AeWorkflow.DialogFooter" TargetType="{x:Type Border}">
        <Setter Property="Padding" Value="12" />
        <Setter Property="Background" Value="{DynamicResource AeBrush.Surface1}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.Border}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
    </Style>

    <Style x:Key="AeWorkflow.DialogBody" TargetType="{x:Type Border}">
        <Setter Property="Padding" Value="16" />
        <Setter Property="Background" Value="{DynamicResource AeBrush.Canvas}" />
    </Style>

    <Style x:Key="AeWorkflow.FailurePanel" TargetType="{x:Type Border}">
        <Setter Property="Padding" Value="12" />
        <Setter Property="Background" Value="{DynamicResource AeBrush.Surface2}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.Danger}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource AeRadius.Surface}" />
    </Style>

    <Style x:Key="AeWorkflow.ProgressHistory" TargetType="{x:Type Border}">
        <Setter Property="Padding" Value="8" />
        <Setter Property="Background" Value="{DynamicResource AeBrush.Surface1}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource AeRadius.Control}" />
    </Style>

</ResourceDictionary>
