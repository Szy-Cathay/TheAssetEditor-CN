# AI 领域词汇表

仅在任务涉及产品身份、Pack、文件夹工程或 glTF 外部资产导入语义时读取。本文件用于消歧，不描述实现；实现以当前代码和测试为准。

| 术语 | 本项目中的唯一含义 | 不要混同为 |
| --- | --- | --- |
| Asset Editor 国区版 | 本仓库交付的中文专用下游产品，运行时身份为 `AssetEditor.CN` | 上游版、英文版 |
| 上游版 | `donkeyProgramming/TheAssetEditor`，仅作为历史来源和功能对照 | 国区版、原版游戏数据 |
| 外部源资产 | 在 Total War Pack 之外制作的模型、骨架、动画或贴图源数据 | 原版游戏数据、用户 Mod Pack |
| 标准 glTF 输入 | 由 Blender 官方 glTF 2.0 导出器产生、不依赖 Asset Editor 专用节点或游戏骨架标记的 `.gltf` 或 `.glb` | Asset Editor 导出后用于回导的 glTF |
| 标准 glTF 材质 | 以 glTF 2.0 PBR 的基础颜色、法线、金属度/粗糙度和透明模式为可靠语义来源的材质输入；存在无法安全转换的 `BLEND` 真半透明材质时，整次跳过材质与贴图并继续其他已选内容 | 通过文件名或任意 Blender 节点图推测出的游戏材质 |
| 外部自有骨架 | 外部源资产携带的骨骼层级和命名，导入时用于生成新的游戏骨架 | 自动映射到已有游戏骨架 |
| 自动人形缩放 | 导入时可选地用一个统一倍率将外部人形的模型、骨架和动画位移同步匹配 `humanoid01` 参考尺寸 | 单骨骼缩放、骨架映射或动画重定向 |
| 批量动画重定向 | 对多个选定动画一次性应用相同的源骨架到目标骨架转换，并保存为相互独立的动画资源 | 只导出未重定向的动画、把多个动画合成一个文件 |
| 游戏材质贴图 | 为 Total War 材质着色器准备的颜色、橙色法线、MaterialMap 及中性缺省贴图 | Blender 材质、glTF PBR 贴图的原样复制 |
| 原子导入 | 外部资产全部通过预检查后才一次性写入当前文件夹工程；任一必要输出失败时不留下模型、骨架、动画或贴图半成品 | 已写入部分文件后再报告其余步骤失败 |
| Pack | Total War 资源归档；资源由游戏内部相对路径标识 | 普通压缩包、工程目录 |
| CA Pack | 随游戏提供、供查找和引用但不直接修改的 Pack | 文件夹工程、用户 Mod |
| 资源路径 | Pack 或文件夹工程内的规范化相对路径 | Windows 绝对路径 |
| 原版游戏数据 | 从当前 Total War 安装目录加载并合并的 CA Pack 资源集合 | 上游版、用户 Mod |
| 文件夹工程 | 将 Mod 资源映射到普通目录并自动保留工程历史的工程；是唯一面向用户的可编辑工作区 | 临时解压目录、任意文件夹、普通可编辑 Pack |
| 参考 Pack | 以只读角色加载、用于浏览和向当前文件夹工程复制资源的 Pack | 可直接修改或保存的工作区、输出 Pack |
| 输出 Pack | 根据文件夹工程当前磁盘内容生成且位于工程外的 `.pack` | 工程备份、Git 提交 |
| 工程历史 | 自动记录工程磁盘状态的本地还原点；普通界面只提供初始化、创建、查看、恢复和放弃未记录修改，底层 `.git` 仅是内部存储实现 | GitHub 同步、云备份、编辑器撤销 |
| 未保存编辑 | 仍只存在于编辑器内存、尚未写入工程磁盘的修改；工程历史不能记录它 | 未记录修改 |
| 未记录修改 | 已写入工程磁盘，但尚未进入当前还原点的新增、修改、删除、重命名或无法读取状态 | 编辑器内存中的未保存编辑 |

若新术语不能防止真实的代码或交互误判，不要添加。
