<Window x:Class="CommonControls.SelectionListDialog.SelectionListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommonControls.SelectionListDialog"
        xmlns:behaviors="clr-namespace:Shared.Ui.Common.Behaviors"
        xmlns:s="http://schemas.singulink.com/xaml"
        xmlns:loc="clr-namespace:Shared.Ui.Common"
        mc:Ignorable="d"
    Title="{Binding WindowTitle}" Height="450" Width="800"
    Background="{DynamicResource AeBrush.Canvas}"
    Foreground="{DynamicResource AeBrush.TextPrimary}"
    FontFamily="{DynamicResource AppFontFamily}"
    FontWeight="{DynamicResource AppFontWeight}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Shared.Ui;component/Common/Styles/EditorWorkspaceStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <local:SelectionListView/>
        
        <DockPanel Grid.Row="1">
            <Button DockPanel.Dock="Left" Content="{loc:Loc Shared.SelectionList.SelectAll}" Margin="2" Width="80" Click="{s:MethodBinding SelectAll}" Style="{StaticResource AeButton.Quiet}" />
            <Button DockPanel.Dock="Left" Content="{loc:Loc Shared.SelectionList.DeselectAll}" Margin="2" Width="80" Click="{s:MethodBinding DeselectAll}" Style="{StaticResource AeButton.Quiet}" />
            <Button DockPanel.Dock="Right" Content="{loc:Loc General.Ok}" Margin="2" Width="80" Click="OkButton_Click" Style="{StaticResource AeButton.Primary}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource AeForm.Label}" Grid.Column="0" Content="{loc:Loc General.Filter}" VerticalContentAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="SearchTextBox" Height="20" behaviors:TextBoxExtensions.Watermark="{loc:Loc Shared.SelectionList.SearchHint}"/>
            </Grid>
        </DockPanel>

    </Grid>
</Window>
