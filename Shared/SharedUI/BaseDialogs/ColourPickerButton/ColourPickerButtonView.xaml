<UserControl x:Class="Shared.Ui.BaseDialogs.ColourPickerButton.ColourPickerButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Shared.Ui.BaseDialogs.ColourPickerButton" 
             xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:loc="clr-namespace:Shared.Ui.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent"
             Foreground="{DynamicResource AeBrush.TextPrimary}"
             FontFamily="{DynamicResource AppFontFamily}"
             FontWeight="{DynamicResource AppFontWeight}">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/Shared.Ui;component/Common/Styles/EditorWorkspaceStyles.xaml"/>
    </UserControl.Resources>
    <Grid>
        <ToggleButton x:Name="PickerButton" Style="{StaticResource AeEditor.ToggleIcon}"
                      Width="Auto" Height="Auto" Padding="2"
                      ToolTip="{loc:Loc Shared.ColourPicker.Choose}" AutomationProperties.Name="{loc:Loc Shared.ColourPicker.Choose}">
            <Border Width="18" Height="18" BorderBrush="{DynamicResource AeBrush.BorderStrong}" BorderThickness="1">
                <Border.Background><SolidColorBrush Color="{Binding PickedColor}"/></Border.Background>
            </Border>
        </ToggleButton>
        <Popup x:Name="PickerPopup" Opened="PickerOpened" Placement="Bottom" PlacementTarget="{Binding ElementName=PickerButton}"
               IsOpen="{Binding IsChecked, ElementName=PickerButton, Mode=TwoWay}" StaysOpen="False" AllowsTransparency="True">
            <Border Style="{StaticResource AeSurface.Overlay}" Padding="8" Focusable="True" PreviewKeyDown="PickerPreviewKeyDown">
                <colorpicker:StandardColorPicker x:Name="Picker" Width="240" Height="420"
                    Foreground="{DynamicResource AeBrush.TextPrimary}"
                    SmallChange="5" UseHintColor="True" HintColor="Transparent"
                    SelectedColor="{Binding PickedColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    ShowFractionalPart="False" ShowAlpha="False">
                    <colorpicker:StandardColorPicker.Resources>
                        <Style TargetType="TextBox" BasedOn="{StaticResource AeInput.TextBox}">
                            <Setter Property="MinHeight" Value="0"/>
                            <Setter Property="Padding" Value="2,0"/>
                        </Style>
                        <Style TargetType="ComboBox" BasedOn="{StaticResource AeInput.ComboBox}">
                            <Setter Property="MinHeight" Value="0"/>
                            <Setter Property="Padding" Value="4,0"/>
                        </Style>
                    </colorpicker:StandardColorPicker.Resources>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="ColorChanged">
                            <i:InvokeCommandAction Command="{Binding HandleColourChangedCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </colorpicker:StandardColorPicker>
            </Border>
        </Popup>
    </Grid>
</UserControl>
