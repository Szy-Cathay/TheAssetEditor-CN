<Window x:Class="CommonControls.BaseDialogs.ErrorListDialog.ErrorListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:errorlistdialog="clr-namespace:CommonControls.BaseDialogs.ErrorListDialog"
        xmlns:loc="clr-namespace:Shared.Ui.Common"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="520" Width="860"
        MinHeight="420" MinWidth="720"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource AppFontFamily}"
        FontWeight="{DynamicResource AppFontWeight}"
        Background="{DynamicResource AeBrush.Canvas}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <errorlistdialog:ErrorListView Margin="12" />
        <Border Grid.Row="1" Style="{StaticResource AeWorkflow.DialogFooter}">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    MinWidth="104"
                    Margin="0,0,8,0"
                    Content="{loc:Loc Shared.ErrorList.SaveReport}"
                    Click="SaveReport_Click"
                    Style="{StaticResource AeButton.Secondary}" />
                <Button
                    MinWidth="88"
                    Content="{loc:Loc Shared.ErrorList.Close}"
                    Click="Button_Click"
                    IsDefault="True"
                    Style="{StaticResource AeButton.Primary}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
