<UserControl x:Class="Editors.ImportExport.Exporting.Presentation.RmvToGltf.RmvToGltfExporterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Editors.ImportExport.Exporting.Presentation.RmvToGltf"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource AeBrush.Canvas}"
             Foreground="{DynamicResource AeBrush.TextPrimary}"
             FontFamily="{DynamicResource AppFontFamily}"
             FontWeight="{DynamicResource AppFontWeight}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Shared.Ui;component/Common/Styles/EditorWorkspaceStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource AeForm.TextLabel}" Grid.Row="0" Grid.Column="0" Text="{loc:Loc RmvToGltfExporter.ExportTextures}" VerticalAlignment="Center"/>
        <CheckBox HorizontalAlignment="Right" Grid.Row="0" Grid.Column="2"  IsChecked="{Binding ExportTextures, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

        <TextBlock Style="{StaticResource AeForm.TextLabel}" Grid.Row="1" Grid.Column="0" Text="{loc:Loc RmvToGltfExporter.ConvertMaterial}" VerticalAlignment="Center"/>
        <CheckBox HorizontalAlignment="Right" Grid.Row="1" Grid.Column="2"
                  IsEnabled="{Binding ExportTextures, UpdateSourceTrigger=PropertyChanged}"
                  IsChecked="{Binding ConvertMaterialTextureToBlender, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                  ToolTip="{loc:Loc RmvToGltfExporter.ToolTip.ConvertMaterial}"/>

        <TextBlock Style="{StaticResource AeForm.TextLabel}" Grid.Row="2" Grid.Column="0" Text="{loc:Loc RmvToGltfExporter.ConvertNormal}" VerticalAlignment="Center"/>
        <CheckBox HorizontalAlignment="Right" Grid.Row="2" Grid.Column="2"
                  IsEnabled="{Binding ExportTextures, UpdateSourceTrigger=PropertyChanged}"
                  IsChecked="{Binding ConvertNormalTextureToBlue, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                  ToolTip="{loc:Loc RmvToGltfExporter.ToolTip.ConvertNormal}"/>

        <TextBlock Style="{StaticResource AeForm.TextLabel}" Grid.Row="3" Grid.Column="0" Text="{loc:Loc RmvToGltfExporter.ExportAnimations}" VerticalAlignment="Center"/>
        <CheckBox HorizontalAlignment="Right" Grid.Row="3" Grid.Column="2" IsChecked="{Binding ExportAnimations, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
    </Grid>
    
</UserControl>
