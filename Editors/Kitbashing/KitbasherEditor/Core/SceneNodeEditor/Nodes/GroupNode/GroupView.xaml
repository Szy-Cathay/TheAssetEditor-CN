<UserControl x:Class="KitbasherEditor.Views.EditorViews.GroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KitbasherEditor.Views.EditorViews"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="False">
        <TextBlock Text="{loc:Loc Group.GroupInformation}"
                   Style="{DynamicResource Kitbash.PropertyTitle}"
                   DockPanel.Dock="Top"/>

        <DockPanel DockPanel.Dock="Top">
            <Label Style="{StaticResource AeForm.Label}" Width="100" DockPanel.Dock="Left" Content="{loc:Loc Group.GroupName}"/>
            <TextBox Text="{Binding GroupName, UpdateSourceTrigger=LostFocus, Mode=TwoWay}" VerticalAlignment="Center"/>
        </DockPanel>
    </DockPanel>
</UserControl>
