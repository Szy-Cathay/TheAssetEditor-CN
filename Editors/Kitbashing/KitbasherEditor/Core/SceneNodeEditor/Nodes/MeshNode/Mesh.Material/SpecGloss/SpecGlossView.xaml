<UserControl x:Class="Editors.KitbasherEditor.ViewModels.SceneNodeEditor.Nodes.MeshNode.Mesh.WsMaterial.SpecGloss.SpecGlossView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editors.KitbasherEditor.ViewModels.SceneNodeEditor.Nodes.MeshNode.Mesh.WsMaterial.SpecGloss" xmlns:userinterface="clr-namespace:GameWorld.Core.Utility.UserInterface;assembly=GameWorld.Core"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\ModelMaterialResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ContentControl x:Name="Shared" DockPanel.Dock="Top">
        <Expander DockPanel.Dock="Top" IsExpanded="True">
            <Expander.Header>
                <DockPanel DockPanel.Dock="Top" Background="{DynamicResource AeBrush.Surface2}" ToolTip="{loc:Loc SpecGloss.ToolTip}">
                    <Label Content="{loc:Loc SpecGloss.Header}" FontSize="{StaticResource HeaderFontSize}" HorizontalAlignment="Left"/>

                </DockPanel>
            </Expander.Header>

            <DockPanel LastChildFill="False">

                <DockPanel LastChildFill="True" DockPanel.Dock="Top">
                    <Label Style="{StaticResource AeForm.Label}" Width ="{StaticResource labelWidth}" DockPanel.Dock="Left" Content="{loc:Loc SpecGloss.UseAlpha}"/>
                    <CheckBox HorizontalAlignment="Right" Margin="2,0,2,0"  IsChecked="{Binding UseAlpha, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" VerticalContentAlignment="Center"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Top">
                    <Label Style="{StaticResource AeForm.Label}" Width ="{StaticResource labelWidth}" DockPanel.Dock="Left" Content="{loc:Loc SpecGloss.DiffuseMap}"/>
                    <userinterface:ShaderTextureView DataContext="{Binding DiffuseMap }"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Top">
                    <Label Style="{StaticResource AeForm.Label}" Width ="{StaticResource labelWidth}" DockPanel.Dock="Left" Content="{loc:Loc SpecGloss.SpecularMap}"/>
                    <userinterface:ShaderTextureView DataContext="{Binding SpecularMap }"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Top">
                    <Label Style="{StaticResource AeForm.Label}" Width ="{StaticResource labelWidth}" DockPanel.Dock="Left" Content="{loc:Loc SpecGloss.GlossMap}"/>
                    <userinterface:ShaderTextureView DataContext="{Binding GlossMap }"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Top">
                    <Label Style="{StaticResource AeForm.Label}" Width ="{StaticResource labelWidth}" DockPanel.Dock="Left" Content="{loc:Loc SpecGloss.NormalMap}"/>
                    <userinterface:ShaderTextureView DataContext="{Binding NormalMap}"/>
                </DockPanel>

                <DockPanel DockPanel.Dock="Top">
                    <Label Style="{StaticResource AeForm.Label}" Width ="{StaticResource labelWidth}" DockPanel.Dock="Left" Content="{loc:Loc SpecGloss.Mask}"/>
                    <userinterface:ShaderTextureView DataContext="{Binding Mask}"/>
                </DockPanel>
            </DockPanel>
        </Expander>
    </ContentControl>
</UserControl>

