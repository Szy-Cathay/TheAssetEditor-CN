<UserControl x:Class="Editors.KitbasherEditor.ViewModels.SceneNodeEditor.Nodes.MeshNode.Mesh.WsMaterial.Emissive.EmissiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editors.KitbasherEditor.ViewModels.SceneNodeEditor.Nodes.MeshNode.Mesh.WsMaterial.Emissive" 
             xmlns:mathviews="clr-namespace:CommonControls.MathViews;assembly=Shared.Ui" xmlns:behaviors="clr-namespace:Shared.Ui.Common.Behaviors;assembly=Shared.Ui" xmlns:b="http://schemas.microsoft.com/xaml/behaviors" xmlns:userinterface="clr-namespace:GameWorld.Core.Utility.UserInterface;assembly=GameWorld.Core" xmlns:colourpickerbutton="clr-namespace:Shared.Ui.BaseDialogs.ColourPickerButton;assembly=Shared.Ui" xmlns:mathviews1="clr-namespace:Shared.Ui.BaseDialogs.MathViews;assembly=Shared.Ui"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\ModelMaterialResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Expander IsExpanded="True" Margin="{StaticResource MaterialSubsectionMargin}" Background="Transparent" BorderThickness="0">
        <Expander.Header>
            <TextBlock Text="{loc:Loc Emissive.Header}" FontWeight="Normal" Margin="3" ToolTip="{loc:Loc Emissive.ToolTip}"/>
        </Expander.Header>
        <StackPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Texture}" Style="{StaticResource AeForm.Label}"/>
                    <userinterface:ShaderTextureView DataContext="{Binding EmissiveTexture}"/>
                </DockPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Tint}" Style="{StaticResource AeForm.Label}"/>
                    <colourpickerbutton:ColourPickerButtonView Width="20" Height="20" HorizontalAlignment="Left" DataContext="{Binding EmissiveTint}"/>
                </DockPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Strength}" Style="{StaticResource AeForm.Label}"/>
                    <TextBox Width="100" HorizontalAlignment="Left" HorizontalContentAlignment="Right" Text="{Binding EmissiveStrength, UpdateSourceTrigger=LostFocus}"/>
                </DockPanel>
                <DockPanel ToolTip="{loc:Loc Emissive.FresnelStrength.ToolTip}">
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.FresnelStrength}" Style="{StaticResource AeForm.Label}"/>
                    <TextBox Width="100" HorizontalAlignment="Left" HorizontalContentAlignment="Right" Text="{Binding EmissiveFresnelStrength, UpdateSourceTrigger=LostFocus}"/>
                </DockPanel>
                <Grid ToolTip="{loc:Loc Emissive.Tiling.ToolTip}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="1" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Tiling}" Style="{StaticResource AeForm.Label}"/>
                    <UniformGrid Grid.Column="1" Columns="2">
                        <TextBlock Text="{loc:Loc Ui.Axis.U}" Style="{StaticResource AeText.Caption}"/>
                        <TextBlock Text="{loc:Loc Ui.Axis.V}" Style="{StaticResource AeText.Caption}"/>
                    </UniformGrid>
                    <mathviews1:Vector2View Grid.Row="1" Grid.Column="1" DataContext="{Binding EmissiveTiling}"/>
                </Grid>
            <Expander Margin="0,4,0,0" Background="Transparent" BorderThickness="0">
                <Expander.Header>
                    <TextBlock Text="{loc:Loc Emissive.Gradient.Header}" FontWeight="Normal" Margin="3" ToolTip="{loc:Loc Emissive.Gradient.ToolTip}"/>
                </Expander.Header>
                <StackPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Gradient1}" Style="{StaticResource AeForm.Label}"/>
                    <DockPanel LastChildFill="False">
                        <colourpickerbutton:ColourPickerButtonView DockPanel.Dock="Left" Width="20" Height="20" DataContext="{Binding Gradient0}"/>
                        <Label DockPanel.Dock="Left" Margin="6,0,4,0" Content="{loc:Loc Emissive.Time}" Style="{StaticResource AeForm.Label}"/>
                        <TextBox DockPanel.Dock="Left" Width="64" HorizontalContentAlignment="Right"
                                 Text="{Binding GradientTime0, UpdateSourceTrigger=LostFocus}" ToolTip="{loc:Loc Emissive.Gradient.ToolTip}"/>
                    </DockPanel>
                </DockPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Gradient2}" Style="{StaticResource AeForm.Label}"/>
                    <DockPanel LastChildFill="False">
                        <colourpickerbutton:ColourPickerButtonView DockPanel.Dock="Left" Width="20" Height="20" DataContext="{Binding Gradient1}"/>
                        <Label DockPanel.Dock="Left" Margin="6,0,4,0" Content="{loc:Loc Emissive.Time}" Style="{StaticResource AeForm.Label}"/>
                        <TextBox DockPanel.Dock="Left" Width="64" HorizontalContentAlignment="Right"
                                 Text="{Binding GradientTime1, UpdateSourceTrigger=LostFocus}" ToolTip="{loc:Loc Emissive.Gradient.ToolTip}"/>
                    </DockPanel>
                </DockPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Gradient3}" Style="{StaticResource AeForm.Label}"/>
                    <DockPanel LastChildFill="False">
                        <colourpickerbutton:ColourPickerButtonView DockPanel.Dock="Left" Width="20" Height="20" DataContext="{Binding Gradient2}"/>
                        <Label DockPanel.Dock="Left" Margin="6,0,4,0" Content="{loc:Loc Emissive.Time}" Style="{StaticResource AeForm.Label}"/>
                        <TextBox DockPanel.Dock="Left" Width="64" HorizontalContentAlignment="Right"
                                 Text="{Binding GradientTime2, UpdateSourceTrigger=LostFocus}" ToolTip="{loc:Loc Emissive.Gradient.ToolTip}"/>
                    </DockPanel>
                </DockPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Gradient4}" Style="{StaticResource AeForm.Label}"/>
                    <DockPanel LastChildFill="False">
                        <colourpickerbutton:ColourPickerButtonView DockPanel.Dock="Left" Width="20" Height="20" DataContext="{Binding Gradient3}"/>
                        <Label DockPanel.Dock="Left" Margin="6,0,4,0" Content="{loc:Loc Emissive.Time}" Style="{StaticResource AeForm.Label}"/>
                        <TextBox DockPanel.Dock="Left" Width="64" HorizontalContentAlignment="Right"
                                 Text="{Binding GradientTime3, UpdateSourceTrigger=LostFocus}" ToolTip="{loc:Loc Emissive.Gradient.ToolTip}"/>
                    </DockPanel>
                </DockPanel>
                </StackPanel>
            </Expander>
            <Expander Margin="0,4,0,0" Background="Transparent" BorderThickness="0">
                <Expander.Header>
                    <TextBlock Text="{loc:Loc Emissive.Motion.Header}" FontWeight="Normal" Margin="3" ToolTip="{loc:Loc Emissive.Motion.Hint}"/>
                </Expander.Header>
                <StackPanel>
                    <TextBlock Text="{loc:Loc Emissive.Motion.Hint}" Style="{StaticResource AeText.Caption}"
                               TextWrapping="Wrap" Margin="4,0,4,4"/>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Distortion}" Style="{StaticResource AeForm.Label}"/>
                    <userinterface:ShaderTextureView DataContext="{Binding EmissiveDistortionTexture}"/>
                </DockPanel>
                <Grid ToolTip="{loc:Loc Emissive.Direction.ToolTip}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="1" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Direction}" Style="{StaticResource AeForm.Label}"/>
                    <UniformGrid Grid.Column="1" Columns="2">
                        <TextBlock Text="{loc:Loc Ui.Axis.U}" Style="{StaticResource AeText.Caption}"/>
                        <TextBlock Text="{loc:Loc Ui.Axis.V}" Style="{StaticResource AeText.Caption}"/>
                    </UniformGrid>
                    <mathviews1:Vector2View Grid.Row="1" Grid.Column="1" DataContext="{Binding EmissiveDirection}"/>
                </Grid>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.DistortStrength}" Style="{StaticResource AeForm.Label}"/>
                    <TextBox Width="100" HorizontalAlignment="Left" HorizontalContentAlignment="Right" Text="{Binding EmissiveDistortStrength, UpdateSourceTrigger=LostFocus}"/>
                </DockPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.Speed}" Style="{StaticResource AeForm.Label}"/>
                    <TextBox Width="100" HorizontalAlignment="Left" HorizontalContentAlignment="Right" Text="{Binding EmissiveSpeed, UpdateSourceTrigger=LostFocus}"/>
                </DockPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.PulseSpeed}" Style="{StaticResource AeForm.Label}"/>
                    <TextBox Width="100" HorizontalAlignment="Left" HorizontalContentAlignment="Right" Text="{Binding EmissivePulseSpeed, UpdateSourceTrigger=LostFocus}"/>
                </DockPanel>
                <DockPanel>
                    <Label DockPanel.Dock="Left" Width="{StaticResource labelWidth}" Content="{loc:Loc Emissive.PulseStrength}" Style="{StaticResource AeForm.Label}"/>
                    <TextBox Width="100" HorizontalAlignment="Left" HorizontalContentAlignment="Right" Text="{Binding EmissivePulseStrength, UpdateSourceTrigger=LostFocus}"/>
                </DockPanel>
                </StackPanel>
            </Expander>
        </StackPanel>
    </Expander>
</UserControl>
