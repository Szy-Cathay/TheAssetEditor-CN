<UserControl
    x:Class="Editors.AnimationVisualEditors.AnimationWorkbench.AnimationWorkbenchBaseAnimationView"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Editors.AnimationVisualEditors.AnimationWorkbench"
    xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:progress="clr-namespace:Shared.Ui.Common.OperationProgress;assembly=Shared.Ui"
    d:DesignHeight="720"
    d:DesignWidth="1180"
    AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.Title}"
    DataContext="{Binding Controller, ElementName=Root}"
    FocusVisualStyle="{StaticResource AeFocus.Keyboard}"
    Focusable="True"
    FontFamily="{DynamicResource AppFontFamily}"
    FontWeight="{DynamicResource AppFontWeight}"
    Foreground="{DynamicResource AeBrush.TextPrimary}"
    Unloaded="Root_Unloaded"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Shared.Ui;component/Common/Styles/EditorWorkspaceStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Style="{StaticResource AeText.PageTitle}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.Title}" />
            <TextBlock Margin="0,4,0,0" Style="{StaticResource AeText.Caption}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.Subtitle}" TextWrapping="Wrap" />
            <TextBlock Margin="0,8,0,0" Style="{StaticResource AeText.Body}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.WorkflowHint}" TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="Transparent">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Style="{StaticResource AeText.SectionTitle}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.DonorTitle}" />
                        <TextBlock Margin="0,4,0,8" Style="{StaticResource AeText.Caption}" Text="{Binding DonorSummary}" TextWrapping="Wrap" />
                        <Button
                            AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.SelectDonor}"
                            Command="{Binding BrowseDonorAnimationCommand}"
                            Content="{loc:Loc AnimationWorkbench.BaseAnimation.SelectDonor}"
                            Style="{StaticResource AeButton.Primary}" />

                        <Border Margin="0,16,0,0" Padding="0,12,0,0" BorderBrush="{DynamicResource AeBrush.Border}" BorderThickness="0,1,0,0">
                            <StackPanel>
                                <TextBlock Style="{StaticResource AeText.SectionTitle}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.StyleTitle}" />
                                <TextBlock Margin="0,4,0,8" Style="{StaticResource AeText.Caption}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.StyleHint}" TextWrapping="Wrap" />
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="104" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="56" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Label HorizontalContentAlignment="Right" Content="{loc:Loc AnimationWorkbench.BaseAnimation.StyleMode}" Style="{StaticResource AeForm.Label}" />
                                    <ComboBox
                                        Grid.Column="1"
                                        Grid.ColumnSpan="2"
                                        AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.StyleMode}"
                                        DisplayMemberPath="DisplayName"
                                        ItemsSource="{Binding StyleOptions}"
                                        SelectedValue="{Binding StyleMode, Mode=TwoWay}"
                                        SelectedValuePath="Value"
                                        Style="{StaticResource AeInput.ComboBox}"
                                        ToolTip="{loc:Loc AnimationWorkbench.BaseAnimation.StyleHint}" />
                                    <Label Grid.Row="1" Margin="0,8,0,0" HorizontalContentAlignment="Right" Content="{loc:Loc AnimationWorkbench.BaseAnimation.StyleWeight}" Style="{StaticResource AeForm.Label}" />
                                    <Slider
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Margin="0,8,8,0"
                                        AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.StyleWeight}"
                                        Maximum="1"
                                        Minimum="0"
                                        SmallChange="0.05"
                                        TickFrequency="0.05"
                                        Value="{Binding StyleWeight, Mode=TwoWay}" />
                                    <TextBlock Grid.Row="1" Grid.Column="2" Margin="0,8,0,0" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource AeText.Technical}" Text="{Binding StyleWeight, StringFormat={}{0:P0}}" />
                                    <CheckBox
                                        Grid.Row="2"
                                        Grid.Column="1"
                                        Grid.ColumnSpan="2"
                                        Margin="0,8,0,0"
                                        AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.IncludeRootMotion}"
                                        Content="{loc:Loc AnimationWorkbench.BaseAnimation.IncludeRootMotion}"
                                        IsChecked="{Binding IncludeRootMotion, Mode=TwoWay}"
                                        Style="{StaticResource AeInput.CheckBox}"
                                        ToolTip="{loc:Loc AnimationWorkbench.BaseAnimation.IncludeRootMotionHint}" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Margin="0,16,0,0" Padding="0,12,0,0" BorderBrush="{DynamicResource AeBrush.Border}" BorderThickness="0,1,0,0">
                            <StackPanel>
                                <TextBlock Style="{StaticResource AeText.SectionTitle}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.OutputTitle}" />
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.ColumnSpan="2" Style="{StaticResource AeText.Label}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.OutputFolder}" />
                                    <TextBox Grid.Row="1" Margin="0,4,0,0" AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.OutputFolder}" Style="{StaticResource AeInput.TextBox}" Text="{Binding OutputFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding OutputFolder}" />
                                    <Button
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Margin="8,4,0,0"
                                        AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.BrowseOutputFolder}"
                                        Command="{Binding BrowseOutputFolderCommand}"
                                        Content="{loc:Loc AnimationWorkbench.BaseAnimation.BrowseOutputFolder}"
                                        Style="{StaticResource AeButton.Secondary}" />
                                    <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Margin="0,8,0,0" Style="{StaticResource AeText.Label}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.OutputPrefix}" />
                                    <TextBox Grid.Row="3" Grid.ColumnSpan="2" Margin="0,4,0,0" AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.OutputPrefix}" Style="{StaticResource AeInput.TextBox}" Text="{Binding OutputPrefix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Margin="0,8,0,0" Style="{StaticResource AeText.Label}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.AnimationSetOutput}" />
                                    <TextBlock
                                        Grid.Row="5"
                                        Grid.ColumnSpan="2"
                                        Margin="0,4,0,0"
                                        AutomationProperties.HelpText="{loc:Loc AnimationWorkbench.BaseAnimation.AnimationSetHint}"
                                        Style="{StaticResource AeText.Technical}"
                                        Text="{Binding AnimationSetOutputPath}"
                                        TextTrimming="CharacterEllipsis"
                                        ToolTip="{Binding AnimationSetOutputPath}" />
                                    <CheckBox
                                        Grid.Row="6"
                                        Grid.ColumnSpan="2"
                                        Margin="0,8,0,0"
                                        AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.OverwriteExisting}"
                                        Content="{loc:Loc AnimationWorkbench.BaseAnimation.OverwriteExisting}"
                                        IsChecked="{Binding OverwriteExisting, Mode=TwoWay}"
                                        Style="{StaticResource AeInput.CheckBox}" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border
                Grid.Column="2"
                Padding="12,0,0,0"
                Background="Transparent"
                BorderBrush="{DynamicResource AeBrush.Border}"
                BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource AeText.SectionTitle}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.CandidateTitle}" />
                    <DataGrid
                        Grid.Row="1"
                        Margin="0,12,0,0"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        IsReadOnly="False"
                        ItemsSource="{Binding Items}"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                        SelectionMode="Single"
                        Style="{StaticResource AeTable.Grid}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="44" Header="{loc:Loc AnimationWorkbench.BaseAnimation.Selected}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox
                                            HorizontalAlignment="Center"
                                            AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.Selected}"
                                            IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Style="{StaticResource AeInput.CheckBox}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="112" Header="{loc:Loc AnimationWorkbench.BaseAnimation.Role}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox
                                            AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.Role}"
                                            DisplayMemberPath="DisplayName"
                                            ItemsSource="{Binding DataContext.RoleOptions, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                            SelectedValue="{Binding Role, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            SelectedValuePath="Value"
                                            Style="{StaticResource AeInput.ComboBox}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="*" Header="{loc:Loc AnimationWorkbench.BaseAnimation.SourcePath}" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            Style="{StaticResource AeText.Technical}"
                                            Text="{Binding SourcePath}"
                                            TextTrimming="CharacterEllipsis"
                                            ToolTip="{Binding SourcePath}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Width="88" Binding="{Binding StatusText}" Header="{loc:Loc AnimationWorkbench.BaseAnimation.Status}" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border
                        Grid.Row="2"
                        Margin="0,8,0,0"
                        Padding="0,8,0,0"
                        Background="Transparent"
                        BorderBrush="{DynamicResource AeBrush.Border}"
                        BorderThickness="0,1,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="56" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Style="{StaticResource AeText.Label}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.SourcePath}" />
                            <TextBlock Grid.Column="1" Style="{StaticResource AeText.Technical}" Text="{Binding SelectedItem.SourcePath}" TextTrimming="CharacterEllipsis" ToolTip="{Binding SelectedItem.SourcePath}" />
                            <TextBlock Grid.Row="1" Margin="0,4,0,0" Style="{StaticResource AeText.Label}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.OutputPath}" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,4,0,0" Style="{StaticResource AeText.Technical}" Text="{Binding SelectedItem.OutputPath}" TextTrimming="CharacterEllipsis" ToolTip="{Binding SelectedItem.OutputPath}" />
                            <TextBlock Grid.Row="2" Margin="0,4,0,0" Style="{StaticResource AeText.Label}" Text="{loc:Loc AnimationWorkbench.BaseAnimation.Details}" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,4,0,0" Style="{StaticResource AeText.Caption}" Text="{Binding SelectedItem.DetailText}" TextWrapping="Wrap" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Style="{StaticResource AeText.Label}" Text="{Binding StatusText}" TextWrapping="Wrap" />
            <Button
                Grid.Column="1"
                AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.Generate}"
                Command="{Binding GenerateSelectedCommand}"
                Content="{loc:Loc AnimationWorkbench.BaseAnimation.Generate}"
                IsEnabled="{Binding CanGenerate}"
                Style="{StaticResource AeButton.Secondary}" />
            <Button
                Grid.Column="3"
                AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.Preview}"
                Command="{Binding PreviewSelectedCommand}"
                Content="{loc:Loc AnimationWorkbench.BaseAnimation.Preview}"
                IsEnabled="{Binding CanPreview}"
                Style="{StaticResource AeButton.Secondary}" />
            <Button
                Grid.Column="5"
                AutomationProperties.Name="{loc:Loc AnimationWorkbench.BaseAnimation.Save}"
                Command="{Binding SaveGeneratedCommand}"
                Content="{loc:Loc AnimationWorkbench.BaseAnimation.Save}"
                IsEnabled="{Binding CanSave}"
                Style="{StaticResource AeButton.Primary}" />
        </Grid>

        <progress:OperationProgressWindowHost
            Grid.RowSpan="3"
            CancelCommand="{Binding ActiveCancelCommand}"
            CancelText="{loc:Loc General.Cancel}"
            CurrentDetailText="{Binding ProgressDetail}"
            IsOperationActive="{Binding IsBusy}"
            IsProgressIndeterminate="{Binding IsProgressIndeterminate}"
            ProgressMaximum="{Binding ProgressMaximum}"
            ProgressValue="{Binding ProgressValue}"
            StatusText="{Binding StatusText}"
            WindowTitle="{loc:Loc AnimationWorkbench.BaseAnimation.ProgressTitle}" />
    </Grid>
</UserControl>
