<UserControl
    x:Class="Editors.AnimationVisualEditors.AnimationWorkbench.AnimationWorkbenchMetaDataView"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="560"
    d:DesignWidth="900"
    AutomationProperties.Name="{loc:Loc AnimationWorkbench.MetaData.Title}"
    FocusVisualStyle="{StaticResource AeFocus.Keyboard}"
    Focusable="True"
    FontFamily="{DynamicResource AppFontFamily}"
    FontWeight="{DynamicResource AppFontWeight}"
    Foreground="{DynamicResource AeBrush.TextPrimary}"
    Loaded="Root_Loaded"
    Unloaded="Root_Unloaded"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Shared.Ui;component/Common/Styles/EditorWorkspaceStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Margin="12" Padding="16" Style="{StaticResource AeSurface.Panel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Style="{StaticResource AeText.PageTitle}" Text="{loc:Loc AnimationWorkbench.MetaData.Title}" />
                    <TextBlock Margin="0,4,0,0" Style="{StaticResource AeText.Caption}" Text="{loc:Loc AnimationWorkbench.MetaData.Subtitle}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Style="{StaticResource AeText.Label}" Text="{loc:Loc AnimationWorkbench.MetaData.Synchronize}" />
                    <ToggleButton
                        x:Name="SynchronizationToggle"
                        AutomationProperties.Name="{loc:Loc AnimationWorkbench.MetaData.Synchronize}"
                        Click="SynchronizationToggle_Click"
                        IsChecked="{Binding IsSynchronizationEnabled, Mode=OneWay}"
                        Style="{StaticResource AeInput.Switch}" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Margin="0,16,0,12" Padding="12" Style="{StaticResource AeSurface.Control}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Style="{StaticResource AeText.SectionTitle}" Text="{loc:Loc AnimationWorkbench.MetaData.Summary}" />
                        <TextBlock Margin="0,4,0,0" Style="{StaticResource AeText.Caption}" Text="{Binding SummaryText}" />
                    </StackPanel>
                    <Border Grid.Column="1" Padding="8,4" VerticalAlignment="Center" Background="{DynamicResource AeBrush.Surface2}" BorderBrush="{DynamicResource AeBrush.Border}" BorderThickness="1" CornerRadius="{StaticResource AeRadius.Control}">
                        <TextBlock Style="{StaticResource AeText.Label}" Text="{Binding DirtyStateText}" />
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Row="2" Padding="12" Style="{StaticResource AeSurface.Control}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Margin="0,0,0,8" Style="{StaticResource AeText.SectionTitle}" Text="{loc:Loc AnimationWorkbench.MetaData.Problems}" />
                    <ListBox
                        x:Name="ProblemList"
                        Grid.Row="1"
                        AutomationProperties.Name="{loc:Loc AnimationWorkbench.MetaData.ProblemsAutomationName}"
                        ItemsSource="{Binding Problems}"
                        ScrollViewer.CanContentScroll="True"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        SelectionChanged="ProblemList_SelectionChanged"
                        Style="{StaticResource AeList.View}"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource AeList.Item}">
                                <Setter Property="AutomationProperties.Name" Value="{Binding AutomationName}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsError}" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource AeBrush.Danger}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource AeBrush.Warning}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Margin="0,0,8,0" VerticalAlignment="Top">
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource AeFeedback.Icon}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsError}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource AeBrush.Danger}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource AeBrush.Warning}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <StackPanel Grid.Column="1">
                                        <DockPanel LastChildFill="True">
                                            <TextBlock Margin="0,0,8,0" DockPanel.Dock="Left" Style="{StaticResource AeText.Label}" Text="{Binding SeverityText}" />
                                            <TextBlock Style="{StaticResource AeText.Body}" Text="{Binding ReasonText}" TextTrimming="CharacterEllipsis" />
                                        </DockPanel>
                                        <TextBlock Foreground="{DynamicResource AeBrush.TextMuted}" Style="{StaticResource AeText.Caption}" Text="{Binding ContextText}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Grid Grid.Row="3" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Style="{StaticResource AeText.Caption}" Text="{loc:Loc AnimationWorkbench.MetaData.SelectProblemHint}" TextWrapping="Wrap" />
                <Button
                    x:Name="NavigateButton"
                    Grid.Column="1"
                    AutomationProperties.Name="{loc:Loc AnimationWorkbench.MetaData.Navigate}"
                    Click="NavigateButton_Click"
                    Content="{loc:Loc AnimationWorkbench.MetaData.Navigate}"
                    IsEnabled="False"
                    Style="{StaticResource AeButton.Quiet}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
