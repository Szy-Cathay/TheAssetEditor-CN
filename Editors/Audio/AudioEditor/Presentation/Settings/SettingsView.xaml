<UserControl 
    x:Class="Editors.Audio.AudioEditor.Presentation.Settings.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:sys="clr-namespace:System;assembly=netstandard"
    xmlns:ExtendedWpfToolkit="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
    xmlns:ValueConverters="clr-namespace:Editors.Audio.Shared.UI.ValueConverters"
    xmlns:Settings="clr-namespace:Editors.Audio.AudioEditor.Presentation.Settings"
    xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
    d:DataContext="{d:DesignInstance Type=Settings:SettingsViewModel}"
    mc:Ignorable="d"
    d:DesignHeight="550"
    d:DesignWidth="960">

    <UserControl.Resources>
        <BooleanToVisibilityConverter 
            x:Key="BoolToVisibilityConverter"/>
        <ValueConverters:EnumConverter 
            x:Key="EnumConverter"/>
        <DataTemplate 
            x:Key="EnumToStringTemplate">
            <TextBlock 
                Text="{Binding Converter={StaticResource EnumConverter}}"/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid 
        Visibility="{Binding IsSettingsVisible, Converter={StaticResource BoolToVisibilityConverter}}">

        <Grid.RowDefinitions>
            <RowDefinition 
                Height="Auto"/>
            <RowDefinition 
                Height="Auto"/>
            <RowDefinition 
                Height="Auto"/>
            <RowDefinition 
                Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="*"/>
        </Grid.ColumnDefinitions>

        <Label 
            Grid.Row="0" 
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="0, 0, 0, 5"
            Content="{loc:Loc AudioSettings.Label}"/>

        <WrapPanel
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Margin="0, 0, 0, 0">

            <Button 
                Grid.Column="0"
                Margin="5, 0, 0, 5" 
                Width="100" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Left"
                Content="{loc:Loc AudioSettings.ResetSettings}"
                ToolTip="{loc:Loc AudioSettings.ResetSettings.ToolTip}"
                Command="{Binding ResetSettingsCommand}"/>

            <Button 
                Grid.Column="1"
                Width="190" 
                Margin="5, 0, 0, 5" 
                VerticalAlignment="Center"
                Content="{loc:Loc AudioSettings.SetRecommendedVO}"
                ToolTipService.ShowOnDisabled="True"
                ToolTip="{loc:Loc AudioSettings.SetRecommendedVO.ToolTip}"
                Command="{Binding SetRecommendedVOSettingsCommand}"
                IsEnabled="{Binding IsSetRecommendedVOSettingsEnabled}"/>

            <Button 
                Grid.Column="2"
                Width="130" 
                Margin="5, 0, 0, 5" 
                VerticalAlignment="Center"
                Content="{loc:Loc AudioSettings.RemoveAudioFiles}"
                ToolTip="{loc:Loc AudioSettings.RemoveAudioFiles.ToolTip}"
                ToolTipService.ShowOnDisabled="True"
                Command="{Binding RemoveSelectedAudioFilesCommand}"
                IsEnabled="{Binding IsRemoveAudioFilesEnabled}">
            </Button>
            
            <CheckBox 
                Grid.Column="3"
                Content="{loc:Loc AudioSettings.ShowSettingsFromViewer}"
                ToolTip="{loc:Loc AudioSettings.ShowSettingsFromViewer.ToolTip}" 
                Margin="5, 0, 0, 5" 
                VerticalAlignment="Center" 
                IsChecked="{Binding ShowSettingsFromAudioProjectViewer, Mode=TwoWay}" 
                HorizontalAlignment="Left"/>
        </WrapPanel>

        <Grid
            Grid.Row="2"
            Grid.Column="0"
            Margin="0, 0, 0, 0">

            <Grid.RowDefinitions>
                <RowDefinition 
                    Height="Auto"/>
                <RowDefinition 
                    Height="Auto"/>
            </Grid.RowDefinitions>

            <Border 
                Grid.Row="0"
                Grid.Column="1"
                BorderBrush="{DynamicResource Button.Static.Border}"
                BorderThickness="0, 0, 0, 0">

                <Expander 
                    x:Name="ExpanderAudioFiles"
                    Style="{StaticResource ExpanderWithBorderBackground}"
                    IsExpanded="True"
                    Padding="0,0,0,0"
                    BorderBrush="{DynamicResource Button.Static.Border}">

                    <Expander.Header>
                        <DockPanel 
                            VerticalAlignment="Center" 
                            LastChildFill="True" 
                            Margin="10, 5, 10, 5">

                            <TextBlock 
                                Text="{loc:Loc AudioSettings.AudioFiles}"
                                VerticalAlignment="Center"
                                Margin="-5, 0, 10, 0" />
                        </DockPanel>
                    </Expander.Header>

                    <ListView
                        x:Name="AudioFilesListView"
                        ItemsSource="{Binding AudioFiles}"
                        Height="135"
                        MaxHeight="135"
                        AllowDrop="True"
                        Drop="OnListViewDrop"
                        DragOver="OnListViewDragOver"
                        MouseDoubleClick="OnAudioFilesListViewMouseDoubleClick"
                        SelectionChanged="OnAudioFilesListViewSelectionChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn 
                                    Header="{loc:Loc AudioSettings.FileName}"
                                    DisplayMemberBinding="{Binding WavPackFileName}"
                                    Width="Auto"/>
                                <GridViewColumn 
                                    Header="{loc:Loc AudioSettings.FilePath}"
                                    DisplayMemberBinding="{Binding WavPackFilePath}"
                                    Width="Auto"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Expander>
            </Border>

            <Border 
                Grid.Row="1"
                Grid.Column="0"
                BorderBrush="{DynamicResource Button.Static.Border}">

                <Expander 
                    Style="{StaticResource ExpanderWithBorderBackground}"
                    IsExpanded="True"
                    Padding="5,0,0,0"
                    BorderBrush="{DynamicResource Button.Static.Border}">

                    <Expander.Header>
                        <DockPanel 
                            VerticalAlignment="Center"    
                            LastChildFill="True"      
                            Margin="10, 5, 10, 5">

                            <TextBlock 
                                Text="{loc:Loc AudioSettings.HircSettings}"
                                VerticalAlignment="Center"
                                Margin="-5, 0, 10, 0"/>
                        </DockPanel>
                    </Expander.Header>

                    <Grid
                        Margin="0, 5, 0, 0">

                        <Grid.RowDefinitions>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                            <RowDefinition 
                                Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition 
                                Width="150"/>
                            <ColumnDefinition 
                                Width="200"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock 
                            Grid.Row="0"
                            Grid.Column="0"
                            Margin="10, 0, 5, 5"
                            Text="{loc:Loc AudioSettings.ContainerType}"
                            ToolTip="{loc:Loc AudioSettings.ContainerType.ToolTip}"
                            Visibility="{Binding IsContainerTypeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ComboBox
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            ItemsSource="{Binding ContainerTypes}" 
                            SelectedItem="{Binding ContainerType}" 
                            ItemTemplate="{StaticResource EnumToStringTemplate}"
                            IsEnabled="{Binding IsContainerTypeEnabled}"
                            Visibility="{Binding IsContainerTypeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock 
                            Grid.Row="1"
                            Grid.Column="0"
                            Margin="10, 0, 5, 5"
                            Text="{loc:Loc AudioSettings.RandomType}"
                            ToolTip="{loc:Loc AudioSettings.RandomType.ToolTip}"
                            Visibility="{Binding IsRandomTypeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ComboBox
                            Grid.Row="1"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            ItemsSource="{Binding RandomTypes}" 
                            SelectedItem="{Binding RandomType}" 
                            ItemTemplate="{StaticResource EnumToStringTemplate}"
                            IsEnabled="{Binding IsRandomTypeEnabled}"
                            Visibility="{Binding IsRandomTypeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <CheckBox
                            Grid.Row="2"
                            Grid.Column="0"
                            Margin="10, 0, 5, 5"
                            Content="{loc:Loc AudioSettings.RepetitionInterval}"
                            IsChecked="{Binding EnableRepetitionInterval, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                            IsEnabled="{Binding IsEnableRepetitionIntervalEnabled}"
                            ToolTip="{loc:Loc AudioSettings.RepetitionInterval.ToolTip}"
                            Visibility="{Binding IsRepetitionIntervalVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ExtendedWpfToolkit:IntegerUpDown 
                            Grid.Row="2"
                            Grid.Column="1"
                            Minimum="1"
                            Maximum="999"
                            ClipValueToMinMax="True"
                            Margin="10, 0, 5, 5"
                            Value="{Binding RepetitionInterval, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                            IsEnabled="{Binding EnableRepetitionInterval}"
                            Visibility="{Binding IsRepetitionIntervalVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock 
                            Grid.Row="3"
                            Grid.Column="0"
                            Margin="10, 0, 5, 5"
                            Text="{loc:Loc AudioSettings.PlaylistEndBehaviour}"
                            ToolTip="{loc:Loc AudioSettings.PlaylistEndBehaviour.ToolTip}"
                            Visibility="{Binding IsPlaylistEndBehaviourVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ComboBox
                            Grid.Row="3"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            ItemsSource="{Binding PlaylistEndBehaviours}" 
                            SelectedItem="{Binding PlaylistEndBehaviour}" 
                            ItemTemplate="{StaticResource EnumToStringTemplate}"
                            IsEnabled="{Binding IsPlaylistEndBehaviourEnabled}"
                            Visibility="{Binding IsPlaylistEndBehaviourVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        
                        <TextBlock 
                            Grid.Row="4"
                            Grid.Column="0"
                            Margin="10, 0, 5, 5"
                            Text="{loc:Loc AudioSettings.PlayMode}"
                            ToolTip="{loc:Loc AudioSettings.PlayMode.ToolTip}"
                            Visibility="{Binding IsPlayModeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ComboBox
                            Grid.Row="4"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            ItemsSource="{Binding PlayModes}" 
                            SelectedItem="{Binding PlayMode}" 
                            ItemTemplate="{StaticResource EnumToStringTemplate}"
                            IsEnabled="{Binding IsPlayModeEnabled}"
                            Visibility="{Binding IsPlayModeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock 
                            Grid.Row="5"
                            Grid.Column="0"
                            Margin="10, 0, 5, 5"
                            Text="{loc:Loc AudioSettings.AlwaysResetContainer}"
                            ToolTip="{loc:Loc AudioSettings.AlwaysResetContainer.ToolTip}"
                            Visibility="{Binding IsAlwaysResetPlaylistVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <CheckBox
                            Grid.Row="5"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            IsChecked="{Binding AlwaysResetPlaylist, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                            IsEnabled="{Binding IsAlwaysResetPlaylistEnabled}"
                            Visibility="{Binding IsAlwaysResetPlaylistVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock 
                            Grid.Row="6"
                            Grid.Column="0"
                            Margin="10, 1, 5, 5"
                            Text="{loc:Loc AudioSettings.LoopingType}"
                            ToolTip="{loc:Loc AudioSettings.LoopingType.ToolTip}"
                            Visibility="{Binding IsLoopingTypeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ComboBox
                            Grid.Row="6"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            ItemsSource="{Binding LoopingTypes}" 
                            SelectedItem="{Binding LoopingType}" 
                            ItemTemplate="{StaticResource EnumToStringTemplate}"
                            IsEnabled="{Binding IsLoopingTypeEnabled}"
                            Visibility="{Binding IsLoopingTypeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock 
                            Grid.Row="7"
                            Grid.Column="0"
                            Margin="10, 1, 5, 5"
                            Text="{loc:Loc AudioSettings.NumberOfLoops}"
                            ToolTip="{loc:Loc AudioSettings.NumberOfLoops.ToolTip}"
                            Visibility="{Binding IsNumberOfLoopsVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ExtendedWpfToolkit:IntegerUpDown
                            Grid.Row="7"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            Minimum="1"
                            Maximum="32767"
                            ClipValueToMinMax="True"
                            Value="{Binding NumberOfLoops, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                            IsEnabled="{Binding IsNumberOfLoopsEnabled}"
                            Visibility="{Binding IsNumberOfLoopsVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock 
                            Grid.Row="8"
                            Grid.Column="0"
                            Margin="10, 1, 5, 5"
                            Text="{loc:Loc AudioSettings.TransitionType}"
                            ToolTip="{loc:Loc AudioSettings.TransitionType.ToolTip}"
                            Visibility="{Binding IsTransitionTypeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ComboBox
                            Grid.Row="8"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            ItemsSource="{Binding TransitionTypes}" 
                            SelectedItem="{Binding TransitionType}" 
                            ItemTemplate="{StaticResource EnumToStringTemplate}"
                            IsEnabled="{Binding IsTransitionTypeEnabled}"
                            Visibility="{Binding IsTransitionTypeVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <TextBlock 
                            Grid.Row="9"
                            Grid.Column="0"
                            Margin="10, 1, 5, 5"
                            Text="{loc:Loc AudioSettings.TransitionDuration}"
                            ToolTip="{loc:Loc AudioSettings.TransitionDuration.ToolTip}"
                            Visibility="{Binding IsTransitionDurationVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ExtendedWpfToolkit:DecimalUpDown  
                            Grid.Row="9"
                            Grid.Column="1"
                            Margin="10, 0, 5, 5"
                            Minimum="0"
                            Maximum="3600"
                            Increment="0.05"
                            ClipValueToMinMax="True"
                            Value="{Binding TransitionDuration, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                            IsEnabled="{Binding IsTransitionDurationEnabled}"
                            Visibility="{Binding IsTransitionDurationVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                </Expander>
            </Border>
        </Grid>

        <Border 
            Grid.Row="3"
            Grid.ColumnSpan="2"
            Height="5" 
            Background="{DynamicResource App.Border}"/>
    </Grid>
</UserControl>
