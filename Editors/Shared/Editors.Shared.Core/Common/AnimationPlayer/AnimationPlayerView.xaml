<UserControl x:Class="AnimationEditor.Common.AnimationPlayer.AnimationPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnimationEditor.Common.AnimationPlayer"
              xmlns:s="http://schemas.singulink.com/xaml"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d"
             d:DesignWidth="800"
             Foreground="{DynamicResource AeBrush.TextPrimary}"
             FontFamily="{DynamicResource AppFontFamily}"
             FontWeight="{DynamicResource AppFontWeight}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Shared.Ui;component/Common/Styles/EditorWorkspaceStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="8,6,8,4">
            <TextBlock Text="{loc:Loc AnimationView.Animation}" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <ToggleButton Style="{StaticResource AeInput.Switch}" IsChecked="{Binding IsEnabled.Value, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,4,0,10" Visibility="{Binding PlayerControlsVisibility.Value, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=".1*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width=".3*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0"  >
                    <Label Content="{loc:Loc SharedAnimPlayer.MainAnimation}" DockPanel.Dock="Left"/>
                    <ComboBox DisplayMemberPath="SlotName.Value" ItemsSource="{Binding PlayerItems, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" SelectedItem="{Binding SelectedMainAnimation, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"   DockPanel.Dock="Right"/>
                </DockPanel>
                 
                <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{loc:Loc SharedAnimPlayer.Frame}"/>
                        <TextBlock Text="{Binding SelectedAnimationCurrentFrame.Value, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text=" / "/>
                        <TextBlock Text="{Binding SelectedAnimationFrameCount.Value, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{loc:Loc SharedAnimPlayer.Time}"/>
                        <TextBlock Text="{Binding SelectedAnimationFps.Value, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="{loc:Loc SharedAnimPlayer.Fps}"/>
                        <TextBlock Text="{Binding SelectedAnimationCurrentTime.Value, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text=" / "/>
                        <TextBlock Text="{Binding SelectedAnimationMaxTime.Value, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </StackPanel>

      

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="2">
                    <Button Style="{StaticResource AeButton.Icon}" Click="{s:MethodBinding SetAnimationFirstFrame}">
                        <Canvas Width="16" Height="16">
                            <Path Data="M 3,3 L 3,13 M 13,3 L 5,8 L 13,13 Z" Stroke="{DynamicResource AeBrush.TextPrimary}" Fill="{DynamicResource AeBrush.TextPrimary}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"/>
                        </Canvas>
                    </Button>
                    <Button Style="{StaticResource AeButton.Icon}" Click="{s:MethodBinding SetAnimationPrivFrame}">
                        <Canvas Width="16" Height="16">
                            <Path Data="M 13,3 L 6,8 L 13,13 Z M 8,3 L 1,8 L 8,13 Z" Fill="{DynamicResource AeBrush.TextPrimary}"/>
                        </Canvas>
                    </Button>
                    <ToggleButton
                        IsChecked="{Binding IsPlaying.Value, Mode=OneWay}"
                        Style="{StaticResource AeEditor.PlaybackToggle}"
                        Click="{s:MethodBinding ToggleAnimationPausePlay}">
                        <Canvas Width="16" Height="16">
                            <Path
                                Data="M 4,2 L 14,8 L 4,14 Z"
                                Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ToggleButton}}}" />
                        </Canvas>
                    </ToggleButton>
                    <Button Style="{StaticResource AeButton.Icon}" Click="{s:MethodBinding SetAnimationNextFrame}">
                        <Canvas Width="16" Height="16">
                            <Path Data="M 3,3 L 10,8 L 3,13 Z M 8,3 L 15,8 L 8,13 Z" Fill="{DynamicResource AeBrush.TextPrimary}"/>
                        </Canvas>
                    </Button>
                    <Button Style="{StaticResource AeButton.Icon}" Click="{s:MethodBinding SetAnimationLastFrame}">
                        <Canvas Width="16" Height="16">
                            <Path Data="M 3,3 L 11,8 L 3,13 Z M 13,3 L 13,13" Stroke="{DynamicResource AeBrush.TextPrimary}" Fill="{DynamicResource AeBrush.TextPrimary}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"/>
                        </Canvas>
                    </Button>
                </StackPanel>

                <CheckBox IsChecked="{Binding LoopAnimation.Value, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="4" Content="{loc:Loc SharedAnimPlayer.Loop}" HorizontalAlignment="Center"/>
            </Grid>

            <ListView  Grid.Column="2" ItemsSource="{Binding PlayerItems, UpdateSourceTrigger=PropertyChanged}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="{loc:Loc SharedAnimPlayer.Player}" Width="auto">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Label Content="{Binding SlotName.Value, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="{loc:Loc SharedAnimPlayer.Frames}" Width="auto">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Label Content="{Binding MaxFrames.Value, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="{loc:Loc AnimationView.Animation}" Width="auto">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Label Content="{Binding AnimationName.Value, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
