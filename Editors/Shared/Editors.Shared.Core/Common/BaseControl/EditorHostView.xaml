<UserControl x:Class="AnimationEditor.Common.BaseControl.EditorHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:referencemodel="clr-namespace:AnimationEditor.Common.ReferenceModel" 
             xmlns:animationplayer="clr-namespace:AnimationEditor.Common.AnimationPlayer" 
             xmlns:basecontrol="clr-namespace:Editors.Shared.Core.Common.BaseControl"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d"
             Background="{DynamicResource AeBrush.Canvas}"
             Foreground="{DynamicResource AeBrush.TextPrimary}"
             FontFamily="{DynamicResource AppFontFamily}"
             FontWeight="{DynamicResource AppFontWeight}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Shared.Ui;component/Common/Styles/EditorWorkspaceStyles.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Shared.Ui;component/Common/Styles/GridSplitterStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <basecontrol:HostTemplateSelector x:Key="EditorSelector" />
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding LeftColumnWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="{Binding RightColumnWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="1" Grid.Column="0">
            <MenuItem Header="{loc:Loc EditorHost.Menu.General}" IsEnabled="False">
                <MenuItem Header="{loc:Loc EditorHost.Menu.SaveShortcutFile}" />
            </MenuItem>
            <MenuItem Header="{loc:Loc EditorHost.Menu.Camera}">
                <MenuItem Header="{loc:Loc EditorHost.Menu.FocusSelection}" Command="{Binding FocusCameraCommand}"/>
                <MenuItem Header="{loc:Loc EditorHost.Menu.Reset}" Command="{Binding ResetCameraCommand}"/>
            </MenuItem>
            <MenuItem
                Header="{loc:Loc FactionTint.Menu}"
                Command="{Binding OpenFactionColourSettingsCommand}" />
        </Menu>

        <ContentControl Grid.Row="2" Grid.Column="0" Content="{Binding GameWorld, Mode=OneTime}"/>

        <GridSplitter Grid.Row="1" Grid.RowSpan="3" Grid.Column="1"  HorizontalAlignment="Stretch"
                      ResizeBehavior="PreviousAndNext" ShowsPreview="False"
                      Style="{StaticResource AeVerticalGridSplitterStyle}" />
        <Grid Grid.Row="1" Grid.Column="2" Grid.RowSpan="3" >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Expander  Grid.Row="2" IsExpanded="True">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center">
                            <Run FontSize="14" FontWeight="Bold"  Text="{loc:Loc EditorHost.SceneContent}"/>
                        </TextBlock>
                    </StackPanel>
                </Expander.Header>
                
                <ListView ItemsSource="{Binding SceneObjects, UpdateSourceTrigger=PropertyChanged}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate >
                    <DataTemplate>
                        <referencemodel:SceneObjectView />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            </Expander>

            <Rectangle Grid.Row="3" Fill="{DynamicResource AeBrush.Border}" Height="1" Margin="2"/>

            <ScrollViewer
                Grid.Row="4"
                Grid.RowSpan="2"
                VerticalScrollBarVisibility="{Binding EditorContentVerticalScrollBarVisibility}">
                <DockPanel  DockPanel.Dock="Top">
                    <ContentControl Content="{Binding ., UpdateSourceTrigger=PropertyChanged}"       
                                    ContentTemplateSelector="{StaticResource EditorSelector}"/>

                </DockPanel>
            </ScrollViewer>
        </Grid>
 
        <animationplayer:AnimationPlayerView Grid.Row="3" Grid.Column="0" DataContext="{Binding Player, Mode=OneTime}"/>
    </Grid>
</UserControl>
