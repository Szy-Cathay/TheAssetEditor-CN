<UserControl x:Class="Editor.VisualSkeletonEditor.SkeletonEditor.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:s="http://schemas.singulink.com/xaml"
             xmlns:behaviors="clr-namespace:Shared.Ui.Common.Behaviors;assembly=Shared.Ui"
             xmlns:mathviews="clr-namespace:CommonControls.MathViews;assembly=Shared.Ui"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d"
             Background="{DynamicResource AeBrush.Canvas}"
             Foreground="{DynamicResource AeBrush.TextPrimary}"
             FontFamily="{DynamicResource AeFont.Family}"
             FontWeight="{DynamicResource AeFont.Weight.Normal}"
             d:DesignHeight="850" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource AeButton.Secondary}" />
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource AeInput.TextBox}" />
        <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource AeInput.CheckBox}" />
        <Style TargetType="{x:Type TreeView}" BasedOn="{StaticResource AeTree.View}" />
        <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource AeTree.Item}" />
        <Style TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextSecondary}" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="{x:Type GroupBox}">
            <Setter Property="Foreground" Value="{DynamicResource AeBrush.TextSecondary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AeBrush.Border}" />
            <Setter Property="Padding" Value="8" />
        </Style>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <DockPanel Margin="12" LastChildFill="False">
        <Label DockPanel.Dock="Top" Content="{loc:Loc SkeletonEditor.Header}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AeBrush.TextPrimary}"/>

        <Grid DockPanel.Dock="Top" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <CheckBox  Grid.Row="0" Grid.Column="0" IsChecked="{Binding ShowSkeleton, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
            <Label     Grid.Row="0" Grid.Column="1" Content="{loc:Loc SkeletonEditor.Skeleton}"/>
            <Label     Grid.Row="0" Grid.Column="2" Content=":"/>
            <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding SkeletonName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
            <Button    Grid.Row="0" Grid.Column="4" Content="{loc:Loc General.Load}" Click="{s:MethodBinding LoadSkeletonAction}" Margin="1"/>

            <CheckBox  Grid.Row="1" Grid.Column="0" IsChecked="{Binding ShowRefMesh, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
            <Label     Grid.Row="1" Grid.Column="1" Content="{loc:Loc SkeletonEditor.RefMesh}"/>
            <Label     Grid.Row="1" Grid.Column="2" Content=":"/>
            <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding RefMeshName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
            <Button    Grid.Row="1" Grid.Column="4" Content="{loc:Loc General.Load}" Click="{s:MethodBinding LoadRefMeshAction}" Margin="1"/>

        </Grid>

        <TreeView DockPanel.Dock="Top" ItemsSource="{Binding Bones, UpdateSourceTrigger=PropertyChanged}" MaxHeight="400" Height="400" Margin="5,5,5,1">
            <b:Interaction.Behaviors>
                <behaviors:BindableSelectedItemBehavior SelectedItem="{Binding SelectedBone, Mode=TwoWay}" />
            </b:Interaction.Behaviors>

            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource AeTree.Item}">
                    <Setter Property="IsExpanded" Value="True" />
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate >
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Children, UpdateSourceTrigger=PropertyChanged}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding BoneName, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text=" - "></TextBlock>
                        <TextBlock Text="{Binding BoneIndex, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <Grid DockPanel.Dock="Top" Margin="5,0,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.33*"/>
                <ColumnDefinition Width="0.33*"/>
                <ColumnDefinition Width="0.33*"/>
                <ColumnDefinition Width="0.33*"/>
                <ColumnDefinition Width="0.33*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{loc:Loc General.New}"  Click="{s:MethodBinding CreateBoneAction}" Margin="0,0,2,0"/>
            <Button Grid.Column="1" Content="{loc:Loc General.Duplicate}"  Click="{s:MethodBinding DuplicateBoneAction}"  Margin="2,0,2,0"/>
            <Button Grid.Column="2" Content="{loc:Loc General.Delete}" Click="{s:MethodBinding DeleteBoneAction}" Margin="2,0,0,0" Style="{StaticResource AeButton.Danger}"/>
            <Button Grid.Column="3" Content="{loc:Loc General.Copy}"  Click="{s:MethodBinding CopyBoneAction}" Margin="2,0,0,0"/>
            <Button Grid.Column="4" Content="{loc:Loc General.Paste}"  Click="{s:MethodBinding PasteBoneAction}" Margin="2,0,0,0"/>
        </Grid>

        <GroupBox DockPanel.Dock="Top" Margin="5" Header="{loc:Loc AnimReTarget.Settings}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Label      Grid.Row="0" Grid.Column="0" Content="{loc:Loc SkeletonEditor.SourceSkeleton}"/>
                <Label      Grid.Row="0" Grid.Column="1" Content=":"/>
                <TextBox    Grid.Row="0" Grid.Column="2" Text="{Binding SourceSkeletonName, UpdateSourceTrigger=LostFocus, Mode=TwoWay}" VerticalContentAlignment="Center" Margin="2"/>

                <Label      Grid.Row="1" Grid.Column="0" Content="{loc:Loc SkeletonEditor.SelectedBoneScale}"/>
                <Label      Grid.Row="1" Grid.Column="1" Content=":"/>
                <TextBox    Grid.Row="1" Grid.Column="2" Text="{Binding BoneVisualScale, UpdateSourceTrigger=LostFocus, Mode=TwoWay}" VerticalContentAlignment="Center" Margin="2">
                    <b:Interaction.Behaviors>
                        <behaviors:TextBoxInputBehavior InputMode="DecimalInput"/>
                    </b:Interaction.Behaviors>
                </TextBox>

                <Label      Grid.Row="2" Grid.Column="0" Content="{loc:Loc SkeletonEditor.Focus}"/>
                <Label      Grid.Row="2" Grid.Column="1" Content=":"/>
                <Button     Grid.Row="2" Grid.Column="2" Content="{loc:Loc SkeletonEditor.FocusSelectedBone}" VerticalContentAlignment="Center"  Click="{s:MethodBinding FocusSelectedBoneAction}" Margin="2"/>

                <Label      Grid.Row="3" Grid.Column="0" Content="{loc:Loc SkeletonEditor.WorldTransforms}"/>
                <Label      Grid.Row="3" Grid.Column="1" Content=":"/>
                <CheckBox   Grid.Row="3" Grid.Column="2" IsChecked="{Binding ShowBonesAsWorldTransform, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>

                <Label      Grid.Row="4" Grid.Column="0" Content="{loc:Loc SkeletonEditor.IsTechSkeleton}"/>
                <Label      Grid.Row="4" Grid.Column="1" Content=":"/>
                <CheckBox   Grid.Row="4" Grid.Column="2" IsChecked="{Binding IsTechSkeleton, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
            </Grid>
        </GroupBox>

        <GroupBox DockPanel.Dock="Top" Margin="5" Header="{loc:Loc SkeletonEditor.SelectedBone}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0"   Grid.Column="0" Content="{loc:Loc SkeletonEditor.Name}"/>
                <Label  Grid.Row="0"  Grid.Column="1" Content=":"/>
                <TextBox Grid.Row="0"  Grid.Column="2" Text="{Binding SelectedBoneName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" VerticalContentAlignment="Center" Margin="2"/>

                <Label      Grid.Row="1" Grid.Column="0" Content="{loc:Loc SkeletonEditor.Translation}"/>
                <Label      Grid.Row="1" Grid.Column="1" Content=":"/>
                <mathviews:Vector3View Grid.Row="1" Grid.Column="2" VerticalContentAlignment="Center" Vector3="{Binding SelectedBoneTranslationOffset, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

                <Label      Grid.Row="2" Grid.Column="0" Content="{loc:Loc SkeletonEditor.Rotation}"/>
                <Label      Grid.Row="2" Grid.Column="1" Content=":"/>
                <mathviews:Vector3View Grid.Row="2" Grid.Column="2" VerticalContentAlignment="Center" Vector3="{Binding SelectedBoneRotationOffset, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>


                <Label Grid.Row="3" Grid.Column="0" Content="{loc:Loc SkeletonEditor.LengthScale}"/>
                <Label Grid.Row="3" Grid.Column="1" Content=":"/>
                <DockPanel Grid.Row="3" Grid.Column="2">
                    <Button Content="{loc:Loc SkeletonEditor.Bake}" DockPanel.Dock="Right" Width="60"  Click="{s:MethodBinding BakeSkeletonAction}" Margin="2"/>
                    <TextBox Text="{Binding BoneScale, UpdateSourceTrigger=LostFocus, Mode=TwoWay}" VerticalContentAlignment="Center" Margin="2" DockPanel.Dock="Left"/>
                </DockPanel>
           
            </Grid>
        </GroupBox>

        <Button DockPanel.Dock="Top" Margin="5" Content="{loc:Loc General.Save}" Click="{s:MethodBinding SaveSkeletonAction}" Style="{StaticResource AeButton.Primary}"/>
    </DockPanel>
    </ScrollViewer>
</UserControl>
