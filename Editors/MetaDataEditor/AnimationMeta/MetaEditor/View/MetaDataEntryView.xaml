<UserControl x:Class="Editors.AnimationMeta.Presentation.View.MetaDataEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="240">
    <Grid MinHeight="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView
            Grid.Row="0"
            Margin="4"
            DisplayMemberPath="DisplayName"
            ItemsSource="{Binding Tags, UpdateSourceTrigger=PropertyChanged}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            SelectionMode="Extended"
            SelectedItem="{Binding SelectedTag, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
            Style="{StaticResource AeList.View}"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling">
            <ListView.ItemContainerStyle>
                <Style
                    TargetType="ListViewItem"
                    BasedOn="{StaticResource AeList.Item}">
                    <Setter
                        Property="IsSelected"
                        Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Style.Triggers>
                        <DataTrigger
                            Binding="{Binding IsDecodedCorrectly}"
                            Value="False">
                            <Setter
                                Property="Foreground"
                                Value="{DynamicResource AeBrush.Danger}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Grid Grid.Row="1" Margin="4,0,4,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button
                Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,4,4"
                Command="{Binding NewActionCommand}"
                Content="{loc:Loc MetaData.New}"
                Style="{StaticResource AeButton.Secondary}" />
            <Button
                Grid.Row="0"
                Grid.Column="1"
                Margin="4,0,0,4"
                Command="{Binding DeleteActionCommand}"
                Content="{loc:Loc General.Delete}"
                Style="{StaticResource AeButton.Secondary}" />
            <Button
                Grid.Row="1"
                Grid.Column="0"
                Margin="0,0,4,4"
                Command="{Binding MoveUpActionCommand}"
                Content="{loc:Loc MetaData.Up}"
                Style="{StaticResource AeButton.Secondary}" />
            <Button
                Grid.Row="1"
                Grid.Column="1"
                Margin="4,0,0,4"
                Command="{Binding MoveDownActionCommand}"
                Content="{loc:Loc MetaData.Down}"
                Style="{StaticResource AeButton.Secondary}" />
            <Button
                Grid.Row="2"
                Grid.Column="0"
                Margin="0,0,4,4"
                Command="{Binding CopyActionCommand}"
                Content="{loc:Loc General.Copy}"
                Style="{StaticResource AeButton.Secondary}" />
            <Button
                Grid.Row="2"
                Grid.Column="1"
                Margin="4,0,0,4"
                Command="{Binding PasteActionCommand}"
                Content="{loc:Loc General.Paste}"
                Style="{StaticResource AeButton.Secondary}" />
            <Button
                Grid.Row="3"
                Grid.ColumnSpan="2"
                Command="{Binding SaveActionCommand}"
                Content="{loc:Loc General.Save}"
                Style="{StaticResource AeButton.Primary}" />
        </Grid>
    </Grid>
</UserControl>
